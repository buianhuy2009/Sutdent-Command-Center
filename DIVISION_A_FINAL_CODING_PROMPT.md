# DIVISION_A_FINAL_CODING_PROMPT.md
> **Word-count proof:** run `wc -w DIVISION_A_FINAL_CODING_PROMPT.md` — must show >=10000. Target 12000-15000. Do not delete content. If you edit, re-run `wc -w` and keep >=10000.
> **Repo:** `buianhuy2009/Sutdent-Command-Center` (historic typo `Sutdent` preserved). **Live:** `https://student-command-center.vercel.app`. **Version:** `2.4.2` in `package.json:3`. **Contest portal:** `ai.tainangviet.vn`.

---
# 0. MANDATORY STEP ZERO — SYNC TO NEWEST CODE FIRST (EVERY SESSION, NO EXCEPTIONS)

You are the coder AI rebuilding **Student Command Center / StudentOS** for **Cuoc thi Sang tao tre Quoc gia trong linh vuc Tri tue nhan tao nam 2026, Bang A (THCS, ages 12-15)**.

Before you read code, before you edit code, before you plan, run these exact commands in project root in order. You MUST do this at session start and again before every push, per `AGENTS.md:15-24`:

```bash
git status --short
git fetch origin
git log --oneline HEAD..origin/main
git log --oneline -8
```

If `git log --oneline HEAD..origin/main` is non-empty, the remote moved:

```bash
git pull --no-rebase origin main
```

Then:

```bash
npm install
npm run build
```

Baseline `npm run build` MUST exit `0`. The build script is defined in `package.json:9` as `vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs`. If it fails, inspect the terminal stack, make a surgical fix, retry. Max 3 self-repair loops per `AGENTS.md:8-13`.

**Never force-push. Never `push --force`. Never `push --force-with-lease`.** If newest commit after fetch is not yours, check author/message with `git log --oneline -8`, note whose commit you absorbed, what it touched, and what you adjusted to keep the merge coherent, per `AGENTS.md:17-24`. Example: commit `60b730b fix(merge): drop resurrected expiry banner copy, add multi-agent merge safety rule` removed a resurrected banner; if you see resurrected banner code after pull, delete it again. Example commits `e1b6bcc fix(crash): gate closed lazy modals`, `d8ee652 fix(auth): persist Canvas + Google`, `021c53b feat(auth): add Vercel serverless refresh`, `4640b11 fix(theme): single source of truth`, `a794d2e fix(theme): dark elevation` — keep them, do not change signatures of `src/services/firebase.ts`, `src/services/theme.ts`, `src/services/googleAuth.ts`, `src/services/canvas.ts` without optional params plus updating all call sites.

After any pull or merge, review the incoming diff for coherence: resurrected code you deliberately deleted (banners, legacy themes, dead flows); duplicate state or effects for the same concern (two writers of one localStorage key or DOM class); changed signatures of shared services — keep backward compatible. Then re-verify: `npm run build` again. Only push when merged tree builds with exit code `0`. Then `git push origin main` and report build passed plus pushing to Vercel. Vercel auto-deploys from `main`.

After Step Zero, reconfirm these files exist with real paths — never invent paths, keys, or functions. Every claim you make in code comments, dossier, or Prompt Log must cite a real `file:line`:

- `package.json` (version `2.4.2` at line 3, scripts `build/lint/test` at lines 6-14, deps React `19.0.1`, Vite `6.2.3`, Tailwind `4.1.14`, Firebase `12.18.0`, Dexie `4.4.5`, Zustand `5.0.15`, Express `4.21.2`, `vite-plugin-pwa` `1.3.0`)
- `AGENTS.md` (24 lines total, surgical edits at line 4, verification loop at lines 8-13, merge safety at lines 15-24)
- `src/App.tsx` (3666 lines, orchestrator, `lazyRetry` at lines 21-36, about 40 lazy workspaces at lines 37-100, `WorkspaceId` import at line 128, `activeWorkspace` at lines 384-393, `OnboardingChecklist` lazy at line 92 and usage near line 3267)
- `src/components/DashboardHome.tsx` (550 lines, greeting at 56-61, name at 64-79, intention at 82-93, vibe at 96-103, sprint via Zustand at 106-109, streak at 112-120, `useNasaApod()` at line 17 and 175, apod mode `scc_nasa_apod_mode` at 177-182, card and wallpaper at 189 and 485-535)
- `src/components/OnboardingChecklist.tsx` (116 lines, keys `scc_onboarding_checks_v1` at line 28, `scc_onboarding_collapsed_v1` at 9 and 32-38, dismissed at 10 and 39-41, auto-detect at 55-67, `getOnboardingProgress` at 13-15, `return null` on 4 of 4 at 69-70)
- `src/hooks/useNasaApod.ts` (80 lines, `isNasaApodEnabled` at 13-15, `setNasaApodEnabled` at 17-22, `load()` at 30-57, `useEffect` listeners at 59-77, return at 79)
- `src/services/publicApis.ts` (261 lines, `NASA_APOD_CACHE_KEY='scc_nasa_apod_cache'` at 170, `NASA_APOD_ENABLED_KEY='scc_enable_nasa_apod'` at 171, `NASA_APOD_TOGGLE_EVENT='scc:apod-toggle'` at 172, env `VITE_NASA_API_KEY` at 176, `DEMO_KEY` fallback at 179, `fetchNasaApodV2()` at 224, wrapper at 260)
- `src/components/AccountSettingsModal.tsx` (import `fetchNasaApodV2` at 53, Gemini test states at 151-152 and 265-281, NASA enabled check at 173, preview states at 175-176, test at 200-207, clear cache at 217, NASA section at about 934-965)
- `src/services/gemini.ts` (1647 lines, key constants at 28-29, `get/setClientGeminiApiKey` at 31-62, `testGeminiApiKey` at 64-76, rate limiter header at 79, Vault AES-GCM at 203-220, Groq key at 255-263, `callGroqDirect` with `llama-3.3-70b-versatile` at 269-297, `callGemini` universal caller at 305-368)
- `src/services/db.ts` (161 lines, Dexie tables v1-v5 at 25-79, new tables `promptLogs/evidenceSnapshots/trainedModels/vaultIndex` at 75-78)
- `src/stores/workspaceStore.ts`, `vite.config.ts` (94 lines, PWA at 9-54, `manualChunks` vendor, firebase, ai, mermaid, katex, markdown, dnd, dexie at 75-86, `chunkSizeWarningLimit:600` at 91), `vercel.json` (CSP, HSTS, X-Frame, Referrer, Permissions), `index.html` (SEO, PWA)
- `src/components/workspaces/` inventory (see Section 7), `src/services/aiTrainingPipeline.ts`, `src/data/aiTrainingDatasets.ts`, `src/services/promptLog.ts`, `src/services/ragVault.ts`, `src/services/evidence.ts`

Use Glob `src/**/*.tsx` and search for `OnboardingChecklist|nasa_apod|NasaApod|fetchNasa|APOD|callGemini|Prompt Log` to verify. Do not proceed until Step Zero is green. Record the baseline build output and the latest commit hash in your working notes. Repeat Step Zero every session and before every push.

Your mission is to make StudentOS win Division A: a middle-school student aged 12 to 15 must be able to explain Input, AI processing, and Output in plain Vietnamese, show testing iterations, show an honest Prompt Log, and export an 8-page dossier plus 5-minute and 3-minute videos. A judge must verify all of that in under three minutes. Every workspace must help that mission while preserving all existing study features. Work surgically, verify with builds, log prompts, snapshot evidence, and keep the app fast, offline-capable, accessible, and safe for children.

---
# 1. COMPETITION CONTEXT — BANG A (THCS, 12-15) EXACT FACTS YOU MUST ENCODE

You are building for **Cuoc thi Sang tao tre Quoc gia trong linh vuc Tri tue nhan tao nam 2026**, issued with `01-KH/TWDTN-KHCN ngay 03/7/2026 cua Ban Bi thu Trung uong Doan`. Portal: `ai.tainangviet.vn`. Your app IS the Division A product demo plus dossier evidence machine. Every label, helper, and export must reflect these exact rules. Do not paraphrase away numbers. When in doubt, quote the rule number and date exactly.

**1.1 Eligibility and team composition.** Bang A equals THCS or equivalent age. Per Dieu 1: maximum 3 students per team plus maximum 1 teacher guide (`giao vien huong dan`). One student joins only 01 team and 01 division per season. Age fallback if no school confirmation, studying abroad, online learning, or deferred status: 12 to 15 maps to Bang A; 16 to 18 maps to Bang B; 19 to 22 maps to Bang C, judged at registration deadline. Team composition is fixed after registration; if a member cannot continue for force majeure with valid confirmation, the team continues with remaining members; no replacement of new members between rounds except for special cases decided by the Organizing Committee (Ban To chuc). One registration form only — if a student appears in a province list plus a school list plus a free list, the Committee recognizes only one valid form. Cite this rule text in the Dossier Team tab so judges see you understand eligibility. Include fields for date of birth validation that warns if age at deadline falls outside 12 to 15. Include teacher guide fields but clearly label the teacher as guide only, not implementer, to respect the ban on teachers doing the product instead of students. Include a declaration checkbox that each member joins only one team and one division this season.

**1.2 Dossier for Vong loai Quoc gia and Vong cap tinh.** Key dates: free-team registration from `10/7/2026 to 20/9/2026` on `ai.tainangviet.vn`; province and school dossiers before `30/9/2026`. Bang A dossier per Mau 1: **PDF maximum 08 pages** (`theo mau cua Ban To chuc`). **Video thuyet trinh maximum 05 minutes**: problem, idea, AI use, practical value. **Video demo maximum 03 minutes**: operation, main functions, results. You must build a Dossier Builder that enforces an 8-page estimator, 5-minute and 3-minute script timers with word-count guidance, and a Google Drive link validator requiring Anyone with link access. No deployment is required at this stage, but Scratch, MIT App Inventor, and Canva AI links are allowed and should be validated as URLs. The dossier must include a Prompt Log plus evidence images. The dossier material is scored with the same criteria for all teams regardless of registration path (province, school, free, or special). Remind teams to keep file names simple, keep Vietnamese diacritics correct, and keep the Drive folder open before submitting.

**1.3 Capabilities Bang A must demonstrate (encode as a visible checklist in the UI).** The official capability list is: (a) identify a close problem in study, school, family, or community; (b) present reason for choosing the problem and product users; (c) describe input data, how the AI tool or model processes it, and output results; (d) use visual tools, no-code or low-code platforms, image, audio, text recognition, automatic dialogue systems, or simple model-training platforms; (e) test the product, find basic errors, and adjust data, prompts, or design; (f) present the improvement process through trials. Evaluation focus: suitability, closeness, and practical meaning of the chosen problem; basic understanding of the AI usage process; ability to clearly describe the relationship between input, processing, and output; ability to use age-appropriate tools for product goals; creativity, educational value, applicability, and completeness; presentation and explanation ability. Every workspace must therefore show an `Input -> AI -> Output` mini-diagram and a `Why?` chip in plain Vietnamese suitable for ages 12 to 15. Every AI feature must have a Try Example button that works without an API key, plus a Verify button that runs with a key and logs to Prompt Log. Every testing action must offer an Evidence Snapshot button that saves before and after proof to the dossier gallery.

**1.4 Regional round (Vong Khu vuc) details you must encode.** Content: the Committee gives an adjustment, supplement, or feature-perfection request on top of the initial product. Format: present and defend the prepared product, then complete a **6-hour improvement session** (`phien thi cai tien 06 gio`), allowed to use AI, no-code, low-code, and AI-assisted coding per regulations. Dates: **North and Central on 17/10/2026** (North in Hanoi, Central in Da Nang); **South on 10/10/2026** (Ho Chi Minh City). North region includes Bac Ninh, Cao Bang, Dien Bien, Ha Noi, Hai Phong, Hung Yen, Lai Chau, Lang Son, Lao Cai, Ninh Binh, Phu Tho, Quang Ninh, Son La, Thai Nguyen, Tuyen Quang. Central region includes Da Nang, Dak Lak, Gia Lai, Ha Tinh, Hue, Khanh Hoa, Lam Dong, Nghe An, Quang Ngai, Quang Tri, Thanh Hoa. South region includes An Giang, Ca Mau, Can Tho, Dong Nai, Dong Thap, Tay Ninh, TP Ho Chi Minh, Vinh Long. Requirements for Bang A: no mandatory independent online deployment; submit an in-day demo video recorded on the competition day; if the product uses a no-code or low-code platform with hosting such as Scratch, MIT App Inventor, Canva AI, or equivalent, the team may attach an access link. **Video Demo maximum 10 minutes, ALL members must appear, each presents their own task or function.** Plus a **Prompt Log report**. Scoring for Bang A and B: **40 percent initial dossier plus 60 percent regional**. The initial dossier is scored with the same criteria for all teams. Maximum 20 free teams per division per region advance from the national qualifier; tie-break by earlier valid submission timestamp; maximum 50 special teams per mien (Tin hoc tre regional participants and national Math or Informatics prize winners) bypass the qualifier and are not counted in the free quota. Encode the timeline presets and the 40/60 weighting visibly in the Dossier Timeline tab. Encode the 10-minute all-members rule in the video script generator with per-member line assignments.

**1.5 Final round (Vong Chung ket) details you must encode.** Standards: best teams selected from regional. Quota: **Bang A maximum 05 per region (15 total), same for B and C.** Content: dossier check plus product verification; **12-hour improvement challenge**; demo plus defense before the jury; summary and awards. For Bang A, improvement focuses on **explaining the AI process, adjusting a simple function, improving user experience, and presenting lessons learned.** Format Stage 1 (Thuc nghiem, 12 hours, allowed to use AI, no-code, low-code, AI-assisted coding, libraries, APIs, models per Committee) plus Stage 2 (Phan bien before jury, experts, and investors if present). Requirements: **real product link or stable demo** after jury-requested fixes; **maintain stable access for 48 hours before verification** and throughout judging; the improved version must run stably at defense; if the product is down due to team fault, the jury may ask for a live demo; if the team cannot perform it, **operation score may be 0**. Plus Prompt Log. Time and place: **20 to 22/11/2026, Thu do Ha Noi.** Encode the 48-hour stability calculator (enter defense time, show when to freeze changes and start monitoring), the 12-hour task board, and the operation-score warning prominently. Encode the Bang A final focus as four checkboxes: explain AI, adjust simple function, improve UX, present lessons.

**1.6 Dieu 5 compliance rules you must enforce in code and UI copy.** Declaration duty: fully declare AI tools, models, Dataset, libraries, reference code, APIs, external resources, and Prompt Log; state the split between self-built, AI-assisted, and open-source inherited parts; explain each tool role, team contribution, output-verification process, and responsibility for accuracy and safety. Allowed tools: AI tools, large language models, no-code and low-code platforms, open-source libraries, pretrained models, public Datasets, APIs, coding and data helpers IF truthfully declared AND the team proves it understands, verifies, edits, operates, and takes responsibility for the product. Banned behaviors: having someone else take the test for you, hiring someone to build the product, letting a teacher, lecturer, mentor, or outsider do the product instead, submitting copied products, faking Prompt Log, commit history, test data, or demo video, hiding code, Dataset, API, or external resources, illegal use of personal data, products violating law, ethics, privacy, information safety, or social morals. Privacy: no collection, processing, or publication of personal image, voice, study, health, or sensitive data without valid consent; for data related to children, students, and schools, anonymize data and follow Committee guidance. Resources must have clear origin and be permitted for study, research, competition, or demonstration; the team is liable for copyright and legality. Re-evaluation (`phuc khao`) within **02 working days** of official results for each round, only for scores, technical recording errors, or process deviation signs, not for qualitative jury comments without process-violation evidence; the re-evaluation result is final. Team is fixed with no mid-competition replacement except special Committee approval. Encode all of this as a prohibited-behavior self-check with blocking validation plus consent gates plus license picker plus anonymize-by-default image handling.

**1.7 Mau 1 eight sections (your Dossier Builder MUST map one to one).** Section 1 `Van de can giai quyet` (close problem plus reason). Section 2 `Doi tuong su dung` (users plus needs). Section 3 `Du lieu, cau lenh, cong cu AI da su dung` (list plus roles). Section 4 `So do mo ta Input, AI xu ly, Output` (diagram). Section 5 `Hinh anh thu nghiem` (images plus captions). Section 6 `Ket qua trinh dien` (functions, operation, results). Section 7 `Han che va huong cai tien` (limits plus next steps). Section 8 `Lich su cau lenh + Drive link` (Google Drive folder with Anyone with link access, Prompt Log plus evidence). Cover form: 1, 2, or 3 member checkboxes, leader info (full name, date of birth, class and school, commune and province, phone, email), second and third members, teacher or expert guide (full name, work unit, position, phone, email), signatures and date in 2026. Page estimator must warn when exceeding 8 pages. All Vietnamese labels must match exactly. Each editor must show a `What to write` hint in plain language, a character counter, a Try Example filler, and an Add Evidence button. Section 4 must embed the Mermaid diagram builder. Section 5 must pull from the evidence gallery. Section 8 must auto-fill from the Prompt Log store plus validate the Drive URL pattern and sharing setting.

Build the whole app so a 13-year-old can complete sections 1 through 8 without reading a manual, and so a judge can verify Input, AI, Output plus Prompt Log plus testing iterations in under three minutes. Keep language simple, keep buttons large, keep explanations visual, and keep every AI output verifiable and editable by the student.

---
# 2. TECH STACK AND REPO MAP (EXACT VERSIONS, NO INVENTION)

**2.1 Stack from `package.json:1-50`.** `react ^19.0.1` plus `react-dom ^19.0.1` with `Suspense plus React.lazy` via `lazyRetry` in `src/App.tsx:21-36`. `vite ^6.2.3` plus `@vitejs/plugin-react ^5.0.4`. `tailwindcss ^4.1.14` plus `@tailwindcss/vite ^4.1.14`. `firebase ^12.18.0` for app and auth. `dexie ^4.4.5` as local-first source of truth. `zustand ^5.0.15` for stores. `express ^4.21.2` for server.ts. `vite-plugin-pwa ^1.3.0` for installability and offline. Extras: `@dnd-kit/core ^6.3.1`, `@dnd-kit/sortable ^10.0.0`, `@dnd-kit/utilities ^3.2.2`, `@google/genai ^2.4.0`, `canvas-confetti ^1.9.4`, `katex ^0.18.4`, `lucide-react ^0.546.0`, `mermaid ^11.17.2`, `motion ^12.23.24`, `react-markdown ^10.1.0`. Dev dependencies: `typescript ~5.8.2`, `vitest ^2.1.9`, `tsx ^4.21.0`, `esbuild ^0.25.0`, `autoprefixer`, `@types/*`. Scripts: `dev` runs `tsx server.ts` (`package.json:6`), `dev:vite` runs `vite --host`, `dev:all` runs concurrently, `build` runs `vite build` plus `esbuild server` (`package.json:9`), `lint` runs `tsc --noEmit` (`package.json:12`), `test` runs `vitest run` (`package.json:13`). Environment keys verified by grep: `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_APP_ID`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_MEASUREMENT_ID`, `VITE_FIREBASE_MESSAGING_SENDER_ID`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_STORAGE_BUCKET`, `VITE_GEMINI_API_KEY`, `VITE_GROQ_API_KEY`, `VITE_GOOGLE_CLIENT_ID`, `VITE_GOOGLE_PROJECT_ID`, `VITE_NASA_API_KEY`. NASA fallback is `DEMO_KEY` in `src/services/publicApis.ts:179` when the env key is missing. Groq models are `llama-3.3-70b-versatile` primary with `llama-3.1-8b-instant` fallback in `src/services/gemini.ts:295-297`. Gemini model for tests is `gemini-2.0-flash` in `src/services/gemini.ts:68`. Keep all versions unless the Committee or security requires an upgrade, and if you upgrade, update the lockfile, rebuild, and note it in the Changelog.

**2.2 Architecture and orchestration.** `src/App.tsx` with 3666 lines is the orchestrator: `Navbar` imported at line 9, `Sidebar` at line 10, `DashboardHome` at line 11, `ErrorBoundary` at line 12, `useBadgeCounts` at line 13, theme helpers `initTheme`, `setTheme`, `syncDarkToTheme` at line 15. `CHUNK_RETRY_KEY` equals `scc_chunk_reload_v1` at line 20 plus `lazyRetry` retry-once plus hard reload at lines 21-36 handles stale-chunk crashes after fresh deploys. Lazy workspaces at lines 37-100 cover AcademicRadar, StemLab, CreationStudio, RetentionVault, DocumentHub, Desmos, GeoGebra, Excalidraw, PhET, Mermaid, Wolfram, RubricChecker, Feynman, PhotoMath, PdfReader, QuizGenerator, Pomodoro, PeriodicTable, UnitConverter, Arxiv, OpenLibrary, CitationVault, Timetable, ScholarshipTracker, GroupProject, PeerQA, NotionImport, DeadlineGantt, GradeForecaster, ExamMode, InternshipTracker, Budget, HabitSleep, TimetableOptimizer, CodeRunner, ResumeBuilder, PresentationCoach, LabReport, EssayOutliner, ImageOcclusion, FSRS Scheduler Info, Viva, LanguageLab, ZoteroImport, PaperChat, DatasetFinder (all from `NewAppsWorkspaces`), plus StudyRoomPanel, TeacherShareView, LectureCopilot, ApiDocsPanel, ExtensionHelper (all from `collab.tsx`), plus WikipediaLookupModal, StudyCardModal, PortfolioExportModal, MorningCheckInModal, OnboardingChecklist, ModelTrainingWorkspace, FewShotLabWorkspace, CompetitionDossierWorkspace, SplitScreenStudio, DailyRadarTab, GmailRadarTab, AssignmentTrackerTab, CanvasSyncTab. `WorkspaceId` type comes from `src/types` imported at line 128. `activeWorkspace` persisted to `scc_active_workspace_v1` at lines 384-393 via `handleWorkspaceTransition` at line 393. `OnboardingChecklist` is lazy at line 92 and rendered near line 3267 — you will move it per Section 4 without changing its props. Preserve the chunk-retry logic exactly; do not replace `lazyRetry` with plain `lazy`. Preserve the workspace IDs; do not rename them because deep links like `/?w=quiz-generator` and `/?w=canvas` depend on them plus PWA shortcuts in `vite.config.ts:31-35`.

**2.3 Bundle and PWA configuration.** `vite.config.ts:68-92` sets `sourcemap:false`, `chunkSizeWarningLimit:600`, and `manualChunks` splits for `vendor` with react, react-dom, zustand, `firebase` with app and auth, `ai` with genai, `mermaid` with mermaid (about 750k, lazy only in MermaidWorkspace), `katex` with katex, `markdown` with react-markdown, `dnd` with dnd-kit, `dexie` with dexie. Index bundle is about 592k with brotli risk; keep lazy and Suspense for every heavy workspace to avoid regressions. `VitePWA` at lines 9-54 sets name `StudentOS - Command Center`, short name `StudentOS`, description about Canvas, Google Workspace, and AI coaching, theme color `#D97757`, background `#FAF9F5`, display standalone, scope and start_url `/`, categories education and productivity, icons 192 and 512, screenshots for dashboard and mobile, shortcuts for New Task at `/?newTask=1`, Focus Mode at `/?focus=1`, Canvas LMS at `/?w=canvas`, Create Quiz at `/?w=quiz-generator`, Search arXiv at `/?w=arxiv`, plus share_target. Workbox sets glob patterns for js, css, woff2, png, svg, webp, ignores index.html, maximum file size 2MB, navigate fallback to index.html with denylist for api routes, runtime caching NetworkFirst for generativelanguage, googleapis, sheets, gmail, drive with 8 to 10 second timeouts and 50 to 100 entries for 300 seconds, StaleWhileRevalidate for export.arxiv.org and openlibrary.org with 50 entries for 86400 seconds. Keep these; add `api.nasa.gov` StaleWhileRevalidate with one-day expiry if missing. Do not inline large assets; keep lazy imports for confetti, excalidraw, mermaid, and katex fonts.

**2.4 Services map and responsibilities.** `src/services/db.ts:10-80` defines Dexie `StudentOSDatabase` versions 1 through 5, with tables notes, srsCards, briefs, assignmentsQueue, bibliography, assignments, preferences, quota, trash, prompts, tokenUsage, dashboard, plus version 5 new tables `promptLogs`, `evidenceSnapshots`, `trainedModels`, `vaultIndex` at lines 75-78. `src/services/gemini.ts:305-368` defines `callGemini` as User Key to Server `/api/gemini/generate` to Groq `llama-3.3-70b-versatile` to `llama-3.1-8b-instant` fallback. `src/services/aiTrainingPipeline.ts` plus `src/data/aiTrainingDatasets.ts` provide few-shot builders plus accuracy, precision, recall, F1, and confusion-matrix evaluators. `src/services/publicApis.ts:16-120` provides arXiv and OpenLibrary search plus APOD helpers at 170-260. `src/services/promptLog.ts`, `src/services/ragVault.ts` with key `scc_rag_vault_enabled`, `src/services/evidence.ts`, `src/services/vault.ts` with AES-GCM PIN, `src/services/theme.ts`, `src/services/firebase.ts`, `src/services/googleAuth.ts`, `src/services/canvas.ts`, `src/services/i18n.ts` with EN plus VI, `src/services/analytics.ts`, `src/services/srsEngine.ts`, `src/services/bibliography.ts` support study flows. `src/stores/workspaceStore.ts` plus `pomodoroStore` with `scc_user_sprint_goal`, `scc_focus_sessions_log`, `scc_pomo_completed_v1` manage UI and focus state. `src/hooks/useNasaApod.ts:24-79`, `useBadgeCounts`, `useDebouncedCallback`, `useSyncEngine`, `useWorkspaceRouter`, `useKeyboardShortcuts`, `useAppRouter` manage cross-cutting concerns. `vercel.json` headers set Content-Security-Policy with default-src self plus allowlisted scripts, styles, fonts, images, connections, and frames, plus HSTS with 63072000 preload, nosniff, SAMEORIGIN, strict-origin-when-cross-origin referrer, and Permissions-Policy with camera none, microphone self, geolocation none. `index.html` holds SEO and PWA meta. Preserve all service contracts; extend additively with optional params, never break existing callers.

**2.5 Data keys verified by grep (do not rename).** Top counts: `scc_markdown_notes_v1` 14, `scc_user_assignments_v2` 11, `scc_pomo_completed_v1` 9, `scc_pinned_apps_v2` 9, `scc_groq_api_key` 9, `scc_gemini_api_key` 9, `scc_active_workspace_v1` 9, `scc_sidebar_expanded` 7, `scc_recent_tabs_v1` 7, plus `scc_onboarding_checks_v1`, `scc_onboarding_collapsed_v1`, `scc_onboarding_dismissed_date_v1` from `OnboardingChecklist.tsx:9-10 and 28`, plus `scc_enable_nasa_apod` from `publicApis.ts:171`, `scc_nasa_apod_cache` from line 170, `scc_nasa_apod_mode` from `DashboardHome.tsx:178`, plus `scc_user_preferred_name`, `scc_user_daily_intention`, `scc_user_selected_vibe`, `scc_user_sprint_goal`, `scc_focus_sessions_log`, `scc_theme`, `scc_explicit_theme_v1`, `scc_auto_system_theme_v1`, `scc_gemini_api_key_session`, `scc_gemini_daily_quota_v1`, `scc_canvas_settings_v1`, `scc_prompt_log_v1`, `scc_stability_pings_v1`, `scc_rag_vault_enabled`, and Dexie tables above. New keys you may add only if needed: `scc_apod_last_fetch`, `scc_dossier_draft_v1`, `scc_prompt_logs_index`, `scc_evidence_gallery_v1`, `scc_model_lab_v1` — document each in the Appendix with purpose and default. Never create a second key for the same concern; reuse the canonical key and its single writer function.

**2.6 Verification commands for this section.** Run `cat package.json | head -n 50` to confirm versions, `ls src/services/`, `ls src/hooks/`, `ls src/data/`, `grep -n "manualChunks" vite.config.ts`, `cat vercel.json | head -n 60`, and content searches for keys. Paste outputs into your commit message body or PR notes. If any file or key is missing, stop and report; do not invent a replacement path.

---
# 3. GLOBAL ENGINEERING RULES (FROM AGENTS.md, NON-NEGOTIABLE)

**3.1 Surgical edits only.** Per `AGENTS.md:4`: make precise line edits. Do not rewrite full 200-plus line files when modifying small functions. Example: to fix onboarding order, edit only the JSX block near `src/App.tsx:3267` plus add a `details` or `summary` wrapper in `DashboardHome.tsx`, not rewrite the 550-line file. To fix APOD, edit only `src/hooks/useNasaApod.ts:30-57` plus `DashboardHome.tsx:188-535` plus `AccountSettingsModal.tsx:173-217`. If a file exceeds 200 lines, use exact oldString and newString with 5 to 15 lines of context. Violation equals fail. Prefer the Edit tool over Write for existing files. Never write new files unless explicitly required by this prompt (Dossier export helpers are allowed only if you cannot extend existing files). Keep diffs reviewable: one concern per commit, clear message prefix like `fix(home):`, `feat(dossier):`, `fix(apod):`.

**3.2 Verification loop for every change.** Per `AGENTS.md:8-13`: edit targeted files, run `npm run build` in the terminal to verify syntax and bundling, if the build fails inspect the terminal error stack and self-repair with maximum 3 retry loops, once `npm run build` passes with exit code 0 push the code to the GitHub repository, inform the user that the build passed and the update is pushing to Vercel. Also run `npm run lint` (`tsc --noEmit`) for touched files and `npm run test` (`vitest run`) for relevant suites (`divisionA`, `academic`, `gemini`, `persistence`, `publicApis`, `workflows`). After any merge, re-verify build again. Update the `ChangelogModal` entry for user-visible changes with plain-language notes. Never claim counts you did not measure (word counts, bundle sizes, test counts) — run the command (`wc -w`, `du -h`, `vitest run`) and paste output. Never skip the build because the change looks trivial; even Markdown changes in this repo require a build check per team policy to catch accidental edits.

**3.3 Strict TypeScript plus security hardening.** TypeScript `strict:true`. No `any` without justification comment. Sanitize all AI-generated HTML via allowlisted `react-markdown` or DOMPurify; never use `dangerouslySetInnerHTML` with raw AI text. CORS allowlist equals `vercel.json` connect-src only; no new external fetch without adding the domain to Content-Security-Policy plus workbox runtimeCaching. SSRF protection: never fetch arbitrary user URLs server-side without an allowlist; client-side iframes only for `*.desmos.com`, `*.geogebra.org`, `*.phet.colorado.edu`, `*.wolframalpha.com`, `*.canva.com`, `*.excalidraw.com`, `*.notion.so`, `*.figma.com`. PII handling: truncate logged prompts to 300 characters, blur faces and names by default in evidence images, require a consent gate before storing any image or voice. Vault handling: Gemini and Groq keys support plaintext `localStorage`, `sessionStorage` session-only, and AES-GCM PIN encryption via `src/services/gemini.ts:203-220`; always show in the UI that localStorage is visible to any script on the origin and recommend session-only or PIN for shared computers. Never log full keys; mask as `AIza...****`. Never commit keys; use `.env` plus Vercel env vars.

**3.4 Accessibility, mobile, and internationalization standards.** WCAG AA: contrast at least 4.5 to 1, focus outline `#D97757` 2px, `aria-expanded`, `aria-controls`, `aria-label`, `role=progressbar`, `role=status`, `role=alert`, keyboard support for Enter, Space, Escape, touch target minimum 44px height and width (see `OnboardingChecklist.tsx:78 and 99 and 107`). Reduced motion: respect `prefers-reduced-motion` plus `scc_reduce_motion_v1` and `scc_reduced_motion`. Mobile: single column below 640px, no horizontal scroll, sticky primary actions reachable with thumb, large tap targets, readable 16px base. Internationalization: every user-facing string via `src/services/i18n.ts` with EN plus VI; default to VI for Bang A labels (Mau 1 exact wording), EN toggle in Settings. Plain language for ages 12 to 15: short sentences, no jargon without a tooltip, progressive disclosure with `details`, `summary`, Show more, socratic error messages like `Hmm, thu lai nhe — kiem tra ket noi mang va thu lai`, confirm modals for destructive actions, Undo where possible via the trash table, Why chips explaining AI behavior, Try Example buttons that work offline or without keys.

**3.5 Data, offline, and state management.** Dexie is the source of truth; localStorage holds cache and preferences. Every mutation writes Dexie plus `updatedAt` ISO timestamp plus an outbox entry for sync when applicable. New tables already in `src/services/db.ts:75-78` (`promptLogs`, `evidenceSnapshots`, `trainedModels`, `vaultIndex`) must be used; do not create parallel stores in localStorage for the same data. Guest mode must degrade gracefully without Firebase, showing skeletons, empty states, and error states with retry. Rate and quota handling: Gemini 15 RPM token bucket with burst 3 (`gemini.ts:79`), 50 per day quota (`scc_gemini_daily_quota_v1`), Groq failover must be transparent with status indicators. Prompt Log logging: every AI call appends `{timestamp, feature, promptTruncated300, model, tokens, userEdit}` to Dexie `promptLogs` plus exportable JSON. Lazy and Suspense: every workspace via `lazyRetry` in `App.tsx:37-100`; skeleton via `src/components/ui/Skeleton.tsx`; `ErrorBoundary` plus `IframeErrorBoundary` for embeds. AppStore and CommandPalette registration is required for every workspace so students can discover features by searching Vietnamese keywords. No feature is complete without skeleton, empty, error, and success (confetti via `canvas-confetti` lazy) states.

**3.6 Honesty and child-safety copy rules.** Never claim students train large language models from scratch; Track 1 trains tiny on-device classifiers. Never invent competition results, scores, or judge quotes. Never show fake progress or spoofed counts; every number must come from real data or real measurement. For children: anonymize by default, blur faces, redact full names to initials unless consent is checked, require parental or teacher consent for publishing identifiable images or voice, show a privacy notice before any upload, and provide a one-click Clear data action. For AI: always show the split between self-built, AI-assisted, and open-source parts; always allow Verify and Edit; always explain the tool role in one sentence; always log the prompt.

---
# 4. HOME FIX 1 — ONBOARDING CHECKLIST MUST NOT BE FIRST THING

**4.1 Problem statement with file evidence.** `OnboardingChecklist` rendered near `src/App.tsx:3267` currently appears above Today Plan and overview, pushing the greeting plus tasks below the fold. For Bang A judges with a two-minute first impression and for 12-year-olds who need immediate orientation, the first paint must be greeting plus Today Plan, not setup chores. Current component in `src/components/OnboardingChecklist.tsx:26-116` already supports collapse (`ONBOARDING_COLLAPSED_KEY='scc_onboarding_collapsed_v1'` at line 9, `collapsed` state at lines 32-38, `setCollapsedPersist` at lines 43-46), dismiss-for-today (`ONBOARDING_DISMISSED_KEY` at line 10 and lines 47-52), auto-detect for Canvas, Google, task, pomodoro at lines 55-67, progress bar via `getOnboardingProgress` at lines 13-15 and bar at line 89, `return null` on 4 of 4 at line 69 and on dismissed at line 70. Preserve all logic; change placement plus default collapsed behavior plus visual hierarchy. Do not change the four check definitions (Connect Canvas, Connect Google Workspace, Create 1 task, Start Pomodoro) or their detection sources.

**4.2 Required surgical fix (edit only, no full rewrite).** Step 1: In `src/App.tsx` near line 3267, move the `<OnboardingChecklist .../>` JSX below the `<DashboardHome overview plus Today Plan>` block, inside a `details`-styled drawer or a collapsed card section titled `Bat dau (Getting started) — {progress}/4`. Do not change props (`onConnectCanvas`, `onConnectGoogle`, `onCreateTask`, `onStartPomodoro`). Keep the lazy import at line 92. Step 2: In `src/components/OnboardingChecklist.tsx:32-38`, adjust first-visit default so if `localStorage.getItem(ONBOARDING_COLLAPSED_KEY)` is null AND (viewport width below 1024 OR DashboardHome Today Plan has at least 1 item), default `collapsed` to true after first paint using `useEffect` with `requestIdleCallback` fallback to `setTimeout 800ms`, not a blocking empty-deps fetch. Keep first-time desktop with no tasks expanded once only if no tasks exist. Persist the choice to `scc_onboarding_collapsed_v1`. Step 3: Keep the header button at lines 73-88 with `aria-expanded`, `aria-controls="onboarding-checklist-body"`, `ChevronDown` rotation, plus `role=progressbar` with `aria-valuenow`, `valuemin`, `valuemax` at line 89. Ensure keyboard Enter and Space toggle and the target is at least 44px. Step 4: Ensure greeting (`DashboardHome.tsx:56-79`) plus Today Plan with overdue, due-today, and upcoming sections render above the fold with no layout shift; the checklist card keeps its existing styles at line 72 (`bg-white dark:bg-[#1A1917] rounded-2xl border`) but its parent gets `order-last` on mobile and collapsible ordering on desktop. Step 5: Keep auto-detect at lines 55-67 (`hasAnyCanvasSettings()`, `getValidGoogleToken() || hasRefreshToken() || scc_gemini_api_key`, `scc_user_assignments_v2`, `scc_pomo_completed_v1`) — do not break detection. Keep `return null` on 4 of 4 and on dismissed-today. Step 6: Add a `Try Example` link inside the collapsed preview reading `Xem vi du: Tao 1 nhiem vu` that calls `onCreateTask`. Add a `Dismiss for today` button already at lines 105-111; keep it.

**4.3 Detailed acceptance criteria.** Cold load with 3 tasks: greeting plus Today Plan visible without scrolling on 1280 by 720; checklist appears as a single collapsed row reading `Getting started 1/4 completed` with a chevron. Clicking expands to 4 rows with correct done versus pending styles (emerald for done at line 99, neutral with hover border at line 99). `localStorage scc_onboarding_collapsed_v1` toggles true and false correctly. Completing all 4 checks hides the component (returns null). Dismiss-for-today hides until the next calendar date. `npm run build` exits 0. Vitest cases: `getOnboardingProgress({canvas:true,google:false,task:true,pomodoro:false})` equals 2; `isDismissedToday(today,today)` equals true; `isDismissedToday(today,yesterday)` equals false. Manual checks: throttle to 3G, checklist never blocks Largest Contentful Paint; screen reader announces progressbar plus expanded state; all touch targets pass 44px; dark mode borders remain visible; no duplicate checklist renders if both App and Dashboard mount it (ensure single mount).

**4.4 Copy in Vietnamese for ages 12 to 15 plus English fallback.** Title: `Bat dau — 4 buoc nho`. Subtitle: `Lam xong se an. Khong can lam ngay.` Buttons: `Ket noi Canvas`, `Ket noi Google`, `Tao 1 nhiem vu`, `Bat dau Pomodoro`. Dismiss: `An hom nay`. Progress: `{progress}/4 hoan thanh`. Persist EN translations in `i18n.ts` as `Getting started`, `Setup checklist`, `Connect Canvas`, `Connect Google Workspace`, `Create 1 task`, `Start Pomodoro`, `Dismiss for today`. Keep the existing uppercase micro-label at line 81 plus helper at line 82. Ensure diacritics are correct in VI strings.

**4.5 Edge cases to handle.** If `localStorage` is unavailable (private mode), default to collapsed false but do not crash; wrap in try-catch as existing code does. If Canvas settings exist but token expired, still mark canvas true (connection configured) but show a subtle `Can kiem tra lai` hint linking to settings. If Google token exists via refresh token, mark google true without prompting re-login. If assignments exist but all completed, still mark task true (creation demonstrated). If pomodoro completed flag exists, mark pomodoro true. Do not auto-expand the checklist on every load; respect the persisted collapsed value. Do not show the checklist to guest users who dismissed it today. Log checklist interactions to analytics as `onboarding_toggle` with collapsed boolean, not personal data.

---
# 5. HOME FIX 2 — NASA APOD TOGGLE NOT WORKING (5 ROOT CAUSES PLUS 10-STEP REPAIR)

**5.1 Why judges think the toggle is broken (explain in code comments plus Changelog).** Current wiring is correct in isolation but fragile when combined. Root cause 1: empty-deps `load()` plus `useEffect [load]` in `src/hooks/useNasaApod.ts:30-77` fetches only once; if Settings writes `scc_enable_nasa_apod` after Home mounted, Home misses the change unless the `scc:apod-toggle` event fires — but `setNasaApodEnabled` at lines 17-22 dispatches both `storage` and custom events, while some callers write `localStorage` directly without dispatching, so listeners never run. Root cause 2: a dead `IntersectionObserver` on a zero-height sentinel (if APOD is lazy-loaded below the fold with no intrinsic height, the observer never fires, so no fetch is triggered and the section stays blank). Root cause 3: `DEMO_KEY` rate limiting: `src/services/publicApis.ts:179` returns `DEMO_KEY` when `VITE_NASA_API_KEY` is missing; DEMO_KEY rate-limits quickly, so `fetchNasaApodV2()` at line 224 returns null, and `useNasaApod.ts:53-54` shows an error only when no cache exists, leaving a blank card with no retry affordance when cache is stale. Root cause 4: video handling ignored: `DashboardHome.tsx:189` gates wallpaper rendering to `mediaType==='image'`; when APOD of the day is a video, the card section at lines 485-535 shows only a link with no thumbnail, looking broken to students. Root cause 5: missing storage listener feedback and no UI toast: Settings toggle at `AccountSettingsModal.tsx:173` reads `localStorage` directly in some paths; preview states at lines 175-176, test at 200-207, and clear at 217 exist but Home does not optimistically update plus no toast is shown, so the user toggles, sees nothing happen, and assumes the feature is broken. Document these five causes in a code comment above `useNasaApod` and in the Changelog entry.

**5.2 Ten-step repair (surgical, preserve canonical keys and event).** Step 1: Keep canonical keys and event names: `NASA_APOD_ENABLED_KEY='scc_enable_nasa_apod'` (`publicApis.ts:171`), `NASA_APOD_CACHE_KEY='scc_nasa_apod_cache'` (line 170), `NASA_APOD_TOGGLE_EVENT='scc:apod-toggle'` (line 172), display mode `scc_nasa_apod_mode` (`DashboardHome.tsx:178`). Never rename them. Export `isNasaApodEnabled` and `setNasaApodEnabled` from `useNasaApod.ts:13-22` as the single writers; refactor any direct `localStorage.setItem('scc_enable_nasa_apod', ...)` to call `setNasaApodEnabled(val)` so both `storage` and custom events fire. Grep for direct writes and replace. Step 2: Eager fetch with idle callback: in `useNasaApod.ts:59-77`, keep `load()` on mount plus `storage` and toggle listeners, but add `requestIdleCallback(() => load(), {timeout:1500})` fallback plus `document.visibilitychange` revalidation when cache is stale (older than 20 hours). Remove any `IntersectionObserver` gating for APOD; APOD must fetch eagerly but render images lazily with `loading="lazy" decoding="async" referrerPolicy="no-referrer"` already at `DashboardHome.tsx:512`. Step 3: Harden `fetchNasaApodV2()` at `publicApis.ts:224` with 8-second `AbortController` timeout, 2 retries with exponential backoff (400ms then 1200ms), `thumbs=true` param for videos, fallback to yesterday's date on HTTP 429, then to a static starry gradient fallback with title, date, and explanation. Keep `VITE_NASA_API_KEY` override at line 176 plus `DEMO_KEY` default at line 179. Add `scc_apod_last_fetch` timestamp; define `isApodCacheFresh(date)` as same calendar day in local time. Do not cache failures; only cache responses with a valid `url`. Step 4: Cache invalidation discipline: `getApodCache`, `setApodCache`, `clearApodCache` at lines 193-206; `setNasaApodEnabled(false)` clears cache (already at `useNasaApod.ts:19`); Settings Clear button at `AccountSettingsModal.tsx:217` removes `NASA_APOD_CACHE_KEY` plus resets `nasaPreview` plus dispatches the toggle event so Home clears instantly. Add a `Retry` path that bypasses stale cache and forces network. Step 5: Card plus wallpaper modes: preserve `DashboardHome.tsx:177-182` mode state plus `Card` and `Wallpaper` buttons at lines 489-491 with `aria-pressed`. Card shows `img` with max height 72, rounded-xl, border at line 512 with `alt` equal to title or `NASA Astronomy Picture of the Day`, plus title, date, copyright, explanation, HD link, credit at lines 520-533, plus `Read more` clamp at 280 characters at lines 523-525. Wallpaper shows a fixed inset backdrop with blur plus content overlay, only when `mediaType` is image. For video: show `thumbnail_url` when available, else show a centered play card with `Watch video on NASA` link at line 517 plus full explanation and metadata. Never render a broken `img` tag for videos. Step 6: Loading, error, and retry states: skeletons at line 494, error at lines 501-504 plus inline `Retry` at line 535 calling `reloadApod`. When disabled, render nothing and reserve no space to avoid layout shift. Add `role="status"` and `aria-live="polite"` for loading and error regions. Ensure the error message is friendly in VI: `NASA gioi han hoac mat mang. Dang hien anh du phong — bam Thu lai.` plus English fallback. Step 7: Settings UX: keep `AccountSettingsModal.tsx:934-965` NASA section with a controlled toggle switch driven by `isNasaApodEnabled()`, a `Test` button (`handleTestNasa` at lines 200-207 with testing, ok, fail states), a preview thumbnail (`nasaPreview` at lines 176, 192, 205), a `Clear cache` button at line 217, a mode radio for card versus wallpaper, and a credit line `Image credit: NASA APOD`. On toggle ON, dispatch the event plus show a toast `Da bat Anh thien van NASA — dang tai…`; on toggle OFF, clear cache plus show a toast. Ensure the toggle is keyboard accessible with visible focus. Step 8: Performance and accessibility details: keep `loading="lazy"`, `decoding="async"`, `fetchPriority="low"`, `referrerPolicy="no-referrer"` at line 512; add `onError` fallback to a gradient plus Retry button. Ensure contrast for overlay text, focus visibility, and `aria-label="APOD display mode"` at line 489. Ensure images have meaningful alt text and videos have descriptive links. Step 9: Unit plus manual tests: extend `src/services/publicApis.test.ts` to mock fetch with 200, 429, and timeout cases, assert `DEMO_KEY` fallback, cache fresh versus stale logic, and video thumbs handling. Add a `useNasaApod` hook test verifying that dispatching the toggle event triggers reload. Manual matrix must cover DEMO_KEY 429 showing yesterday cache plus Retry, video day showing play card, offline showing stale cache plus amber warning, toggle OFF to ON across two tabs syncing instantly via `storage` event, and Clear cache removing the image. Step 10: Changelog plus docs: add a `ChangelogModal` entry reading `Sua loi cong tac NASA APOD — gio bat/tat cap nhat ngay, co thu lai khi gioi han DEMO_KEY` plus English translation. Update Appendix env docs to explain `VITE_NASA_API_KEY` override and DEMO_KEY limits.

**5.3 Acceptance criteria for APOD.** Toggle ON in Settings makes Home APOD appear within 2 seconds when cache is fresh or within 8 seconds on network, without requiring reload; Toggle OFF makes it disappear instantly plus clears cache. Throttled offline shows a stale image plus the friendly warning plus Retry. Video day shows a play card with no broken image. Two tabs open: toggling in tab A updates tab B via the `storage` event without reload. `npm run build` exits 0. No new keys introduced; reuse canonical keys. No direct localStorage writes bypassing the single writer. All images lazy with correct referrer and alt. All interactive controls meet 44px and focus requirements.

**5.4 Copy for students.** Toggle label: `Anh thien van NASA moi ngay (APOD)`. Description: `Xem anh vu tru kem giai thich tieng Anh co dich tom tat. Can mang. Gioi han mien phi.` Buttons: `Kiem tra ket noi`, `Xoa anh da luu`, `Thu lai`, `Doc them`, `Thu gon`, `Mo anh HD`. Credit: `Anh: NASA APOD`. Error: `NASA gioi han hoac mat mang. Hien anh cu — bam Thu lai.` Ensure EN versions exist in `i18n.ts`.

---
# 6. MORE AI THAN API KEY — TRAIN-THE-MODEL LAB HONEST FOR AGES 12-15 (6 TRACKS, NO FALSE CLAIMS)

You must never claim students train large language models from scratch. Honest framing for every track: Track 1 trains tiny on-device classifiers with a Teachable Machine mental model; Track 2 calibrates few-shot prompts over the existing `callGemini` and Groq pipeline; Track 3 retrieves answers over the student's own notes (RAG vault); Track 4 orchestrates safe, reversible agent actions; Track 5 surfaces multimodal helpers with demo-without-key plus Verify; Track 6 shows dual-provider transparency with status and quota. All tracks must log to Prompt Log and offer Evidence Snapshot. All copy must be plain Vietnamese for middle school with progressive disclosure. No track may require an API key for its demo; keys only unlock live verification.

**6.1 Track 1 — On-device classifier lab (extend `src/components/workspaces/ModelTrainingWorkspace.tsx`).** Preserve the existing training UI plus Dexie `trainedModels` table (`src/services/db.ts:77`). Add support for 2 to 4 labels (example defaults: `Sach Vo`, `But Viet`, `Cap Sach`), enforce at least 8 examples per label with a live counter and a blocking message when below threshold, support webcam capture, file upload, and typed text examples, train in under 30 seconds with a progress bar and cancel, compute metrics `accuracy` plus `confusionMatrix` by reusing evaluators from `aiTrainingPipeline.ts`, save the model descriptor to IndexedDB (`trainedModels` with id, type, savedAt), support export as JSON, and provide an Evidence Snapshot button that saves a sample image plus metrics plus timestamp to `evidenceSnapshots`. Add a consent and ethics gate with a required checkbox reading `To dong y dung anh cua minh de luyen mo hinh nho nay. Khong tai len anh cua ban khac khi chua duoc dong y.` plus an anonymize toggle that blurs faces. Show a plain-language explainer: `May hoc bang vi du. Cang nhieu anh dung, may doan cang gioi. Du lieu dau vao -> AI xu ly -> Ket qua dau ra.` Provide socratic errors (example: `Moi nhan moi chi co 3 anh — them 5 anh nua nhe`), a Try Example button that loads 3 sample images per label from bundled placeholders, and a Why chip explaining training. Acceptance: 2 labels times 8 images trains successfully, accuracy and confusion are shown, model reloads after refresh, snapshot appears in the Dossier gallery, no network required, no key required, build green.

**6.2 Track 2 — Few-shot calibration lab (extend `src/components/workspaces/FewShotLabWorkspace.tsx` plus `src/services/aiTrainingPipeline.ts` plus `src/data/aiTrainingDatasets.ts`).** Preserve existing prompt builders and evaluators. Add 3 to 5 example slots with input and output fields, a Run benchmark button that evaluates over bundled datasets for math, vocabulary, and science, a report showing `accuracy`, `precision`, `recall`, `F1` plus a before-and-after table comparing 0-shot versus few-shot, and an Add to dossier button that copies the report plus prompt history to the Dossier Testing Log. Show `System Prompt` plus full history plus per-run edit notes, and log every run to Dexie `promptLogs` with truncated prompt. Copy must read: `Cho AI xem vai vi du mau, roi cham xem AI lam tot hon khong. Ghi lai de lam bang chung.` Provide demo-without-key using canned model outputs plus Verify-with-key using live `callGemini`. Handle quota exceeded with a friendly message and retry guidance. Acceptance: adding 3 quality examples either improves the toy benchmark or honestly shows no gain with an explanation of why; the report is exportable as text and JSON; the Testing Log receives the entry; build green.

**6.3 Track 3 — RAG vault over own notes (extend `src/services/ragVault.ts` plus DocumentHub, PdfReader, Drive integration).** Key `scc_rag_vault_enabled` already exists; reuse it. Flow: index `scc_markdown_notes_v1` plus Drive and PDF extracted text using client-side chunking with no upload without explicit consent, then answer queries with `Sources: [note title plus snippet plus score]` plus a cited answer. Show the flow as `Input (cau hoi) -> AI (tim doan ghi chep lien quan) -> Output (tra loi kem nguon)`. Provide consent toggle, Clear index button that wipes `vaultIndex` Dexie table (`db.ts:78`), and per-document remove. Provide demo-without-key using local keyword retrieval only, no LLM call. With a key, use `callGemini` to synthesize the answer but always show sources. Empty vault shows an empty state with `Nhap ghi chep` call to action. Acceptance: asking `lich thi toan khi nao` returns the cited note when present; empty vault shows guidance; no key still retrieves snippets; clearing works; all queries logged with truncation; build green.

**6.4 Track 4 — Agent playground with safe reversible actions (extend `src/components/collab.tsx` agent actions plus `AgentAction` type in `src/types`).** Existing safe actions to preserve and verify by Grep (cite lines in code comments): `setWorkspaceLayout`, `injectDesmosEquation`, `createCalendarMilestones`, `createSRSDeck`, `generateMermaidDiagram`. Add 2 new safe, reversible actions: `createTestingChecklist(feature)` which drafts a Testing Log table for the named feature in the Dossier, and `draftPromptLogSummary(feature)` which summarizes `promptLogs` for the feature with 300-character truncation and counts by model. Every agent proposal must show a `ConfirmationModal` (`src/components/ConfirmationModal.tsx`) with a plain-language preview, affected items list, Confirm and Cancel buttons, plus an Undo action that restores prior state via the `trash` table (`db.ts:57`) with expiry, plus an `Explain in plain words` toggle that rewrites the plan for a 12-year-old. Forbid destructive, irreversible, external POST, key exfiltration, or personal-data broadcast actions; hard-block them with a message. Log every proposal, confirmation, and undo to Prompt Log. Acceptance: agent proposes a layout change, user confirms, change applies, Undo restores; summary action produces a readable summary; blocked action shows a clear refusal; build green.

**6.5 Track 5 — Multimodal helpers surface (wrap existing `callGemini` helpers).** Inventory every `callGemini` helper in `src/services/gemini.ts:414-798` covering briefs, syllabus parsing, quizzes, flashcards, rubric pre-check, Feynman feedback, mermaid diagrams, STEM chat, viva practice, math debug, and markdown notes. Each helper card must show: what it does in one Vietnamese sentence, an `Input -> AI -> Output` line, a `Demo without key` button showing canned input and canned output with no network, a `Verify` button that runs live with a key and shows a quota bar, and an `Add to dossier` button that creates an evidence snapshot. Never gate core study features behind a key; demo mode must always work and clearly label canned data as `Vi du mau, chua chay that`. Acceptance: without a key, each helper shows demo output plus `Them API key de thu that` call to action; with a key, it runs, logs, and allows editing before saving; quota UI updates; build green.

**6.6 Track 6 — Dual-provider transparency (extend `src/services/gemini.ts:255-368` plus Settings plus status indicators).** Preserve `getClientGeminiApiKey` and `setClientGeminiApiKey` (`31-62`), `testGeminiApiKey` (`64-76`), Groq key helpers, `callGroqDirect` with `llama-3.3-70b-versatile` primary and `llama-3.1-8b-instant` fallback, and `callGemini` chain User Key to Server to Groq. UI must show status dots (green, yellow, gray, red) for Gemini client, Server proxy (`/api/gemini/generate`), and Groq, plus quota bars for daily 50 and 15 RPM burst 3, plus Vault PIN versus sessionOnly explanations (`sessionStorage scc_gemini_api_key_session` versus `localStorage scc_gemini_api_key` with plaintext warning at line 57). Add a Server proxy indicator with latency. Copy must be honest: `Gemini chinh, Groq du phong. Khoa luu o may ban. Dung PIN hoac che do phien cho may dung chung.` Acceptance: no key shows gray plus demo mode; invalid key makes Test fail with a socratic fix suggestion; quota exceeded shows a friendly message with retry-after; provider switch is logged; build green.

All 6 tracks must include Evidence Snapshot and Prompt Log auto-logging. No track may claim LLM training from scratch; Track 1 help text must state `Mo hinh nho luyen tren may (khong phai LLM lon nhu Gemini)`. All tracks must work in Vietnamese by default with English toggle. All tracks must be discoverable via AppStore and CommandPalette with keywords like `luyen mo hinh`, `vi du mau`, `tim trong ghi chep`, `tro ly tu dong`, `anh van am thanh`, `api key`.

---
# 7. EXHAUSTIVE FEATURE INVENTORY — PRESERVE PLUS SIMPLIFY FOR AGES 12-15

For EVERY feature below, follow this template exactly. Do not skip any. Surgical edits only. Cite primary files with `:line` in code comments and in Dossier evidence where relevant.

**Template per feature (about 220 words of implementation guidance each):** (a) Primary files with real paths. (b) What exists to preserve including keys, tables, slices, styles with exact names. (c) Division A improvements for ages 12 to 15 including plain Vietnamese, Input-AI-Output diagram line, socratic errors, confirm modals for destructive actions, Verify and Edit affordances, Why chip, Try Example button, Evidence Snapshot button. (d) Dataflow plus acceptance including skeletons, empty states, error states with retry, success confetti via `canvas-confetti` lazy, guest-mode degradation, rate and quota handling, Prompt Log logging with 300-char truncation, lazy and Suspense bundling, AppStore and CommandPalette registration with Vietnamese keywords.

**7.1 Navbar (`src/components/Navbar.tsx`).** Primary file Navbar plus theme service `src/services/theme.ts` via `initTheme`, `setTheme`, `syncDarkToTheme` used in `App.tsx:15`. Preserve top navigation, search trigger that opens CommandPalette, account menu, theme toggle, and responsive collapse. Keys `scc_theme`, `scc_explicit_theme_v1`, `scc_auto_system_theme_v1`. Improvements: ensure 44px targets, add VI `aria-label` values, add Why tooltips for AI buttons, collapse labels to icons below 768px, reflect APOD wallpaper state without breaking contrast. Dataflow is local only with no network. Acceptance: `Ctrl+K` opens palette, theme persists across reload, no layout shift, keyboard focus visible, build green. Add analytics for navigation without personal data.

**7.2 Sidebar (`src/components/Sidebar.tsx`).** Primary file Sidebar plus `useBadgeCounts` from `App.tsx:13`. Preserve expand state `scc_sidebar_expanded`, pinned apps `scc_pinned_apps_v2`, recent tabs `scc_recent_tabs_v1`, badge counts, and Play-Store row style with ratings from theme commit `a794d2e`. Improvements: group apps into `Hoc tap`, `Sang tao`, `Nhan thuc`, add age badge `12-15 de hieu`, add Try Example per app that deep-links with `?w=` param, make groups collapsible with `details`. Dataflow is local. Acceptance: pin and unpin persist, active workspace from `scc_active_workspace_v1` highlights correctly, mobile drawer closes on Escape, screen reader announces expanded state, build green.

**7.3 Shell (`src/layouts/Shell.tsx`).** Primary file Shell plus `Skeleton.tsx` plus `ErrorBoundary.tsx`. Preserve layout grid, Suspense fallback, and error containment. Improvements: ensure Home greeting stays above fold, onboarding drawer uses `order-last` on mobile, APOD wallpaper uses negative z-index below content with readable overlay, sidebar and content never overlap on small screens. Dataflow is layout only. Acceptance: Largest Contentful Paint under 3 seconds on throttled 3G, no horizontal scroll at 360px width, reduced-motion respected, build green.

**7.4 CommandPalette (`src/components/CommandPalette.tsx`).** Primary file CommandPalette plus recent key `scc_recent_palette_v1`. Preserve fuzzy search, keyboard navigation, recent items, and workspace actions. Improvements: register every workspace plus Dossier actions (`Tao dan chung`, `Ghi Prompt Log`, `Xuat PDF`), add VI synonyms (`thoi khoa bieu` maps to timetable, `luyen mo hinh` maps to training), add `?` help overlay. Dataflow is local with usage logged to `scc_app_usage_v1`. Acceptance: `Ctrl+K` opens, arrows plus Enter select, Escape closes, search finds Vietnamese keywords, build green.

**7.5 Omnibox (`src/components/Omnibox.tsx`).** Primary file Omnibox plus Dexie outbox. Preserve quick capture that creates tasks or notes. Improvements: placeholder `Nhap bai tap, y tuong…`, hint line `Nhap -> AI goi y -> Luu`, Evidence Snapshot when AI enriches, confirm before bulk import. Dataflow writes to `scc_user_assignments_v2` and Dexie notes with `updatedAt`. Acceptance: offline capture queues, AI enrich logs to Prompt Log, empty and error states shown, build green.

**7.6 FloatingCopilot (`src/components/FloatingAiCopilot.tsx`).** Primary file FloatingCopilot plus `callGemini`. Preserve floating button with context-aware suggestions based on active workspace. Improvements: require confirm before inserting AI text, provide Undo, show quota bar, support demo-without-key with canned tips. Dataflow calls `callGemini` with truncation and logging. Acceptance: no key shows demo tips, with key runs live, insertion is reversible, build green.

**7.7 StudyAssistantChat (`src/components/StudyAssistantChat.tsx`).** Primary file chat plus `callGemini` at `gemini.ts:305`. Preserve conversational tutoring with history. Improvements: socratic mode for ages 12 to 15 that asks guiding questions before giving answers, Why chip per answer, Sources section when RAG is on, 300-char logging, Edit before saving to notes. Dataflow persists chosen answers to Dexie on demand. Acceptance: streams or paginates long answers, error shows retry, history clears with confirm, build green.

**7.8 AppStoreModal (`src/components/AppStoreModal.tsx`).** Primary file store plus pin sync. Preserve catalog, search, install and pin flows. Improvements: keep ratings row, add age badge, add Try Example deep link, add category filters for Bang A relevance (`De thi`, `Luyen AI`, `Bao cao`). Dataflow syncs pins to `scc_pinned_apps_v2` and Sidebar. Acceptance: every workspace listed, search works in VI, pin reflects instantly, build green.

**7.9 AccountSettingsModal (`src/components/AccountSettingsModal.tsx`).** Primary file with 7 sections for general, models, sync, appearance, shortcuts, integrations, support. Preserve Gemini key and Test at lines 151-152 and 265-281, NASA toggle, preview, Test, Clear at 173-217 and 934-965, plus `fetchNasaApodV2` import at 53. Improvements: apply Section 5 APOD fix, add quota bars, Vault PIN explainer, sessionOnly toggle, provider dots from Track 6. Dataflow touches `scc_gemini_api_key`, `scc_groq_api_key`, `scc_enable_nasa_apod`, cache keys. Acceptance: Test shows ok or fail correctly, Clear wipes APOD cache and updates Home, toggle dispatches event, build green.

**7.10 GeminiSuite and AiAcademicSuiteModal (`src/components/AiAcademicSuiteModal.tsx` plus `GeminiSettingsModal.tsx`).** Primary files plus `gemini.ts` helpers. Preserve suite of academic AI tools. Improvements: convert to Track 5 cards with demo-without-key, Verify, Add-to-dossier, plus Track 6 status dots. Dataflow uses `callGemini` with logging. Acceptance: each tool demo works offline, live run logs, build green.

**7.11 LandingPage (`src/components/LandingPage.tsx`).** Primary file plus `index.html` SEO. Preserve hero, features, PWA install prompt. Improvements: add Bang A badge `Du thi Bang A — THCS (12-15)`, Input-AI-Output graphic, portal link `ai.tainangviet.vn`, key dates 17/10, 10/10, 20-22/11, dossier 8-page note. Dataflow is static. Acceptance: Lighthouse SEO at least 90, no invented statistics, links valid, build green.

**7.12 DocumentHub (`src/components/workspaces/DocumentHubWorkspace.tsx`).** Primary file plus Dexie `notes` and `scc_markdown_notes_v1`. Preserve Markdown editing, listing, search. Improvements: RAG index toggle that feeds Track 3, Sources display, diagram hint, Evidence Snapshot. Dataflow is Dexie-first with `updatedAt`. Acceptance: offline edit persists, export works, empty state guides, build green.

**7.13 Timetable workspaces (`src/components/workspaces/TimetableWorkspace.tsx` plus Optimizer in `NewAppsWorkspaces.tsx`).** Primary files plus `scc_timetable_v1`. Preserve grid editing and optimizer suggestions. Improvements: drag with confirm, `Tao moc lich` agent action from Track 4, print view. Dataflow persists locally. Acceptance: changes persist, optimizer logs prompts, build green.

**7.14 PdfReader (`src/components/workspaces/PdfReaderWorkspace.tsx`).** Primary file plus PDF rendering. Preserve open, paginate, annotate, note-link. Improvements: RAG chunk ingest with consent and redaction option, snapshot page to evidence. Dataflow keeps PDFs local where possible. Acceptance: large PDFs lazy-load, no CORS break, annotations persist, build green.

**7.15 Feynman (`src/components/workspaces/FeynmanWorkspace.tsx`).** Primary file plus `ThreeTierFeynmanResult` at `gemini.ts:22`. Preserve explain-simply flow with tiered feedback. Improvements: plain-language rubric for Bang A, Why chip, Try Example topic (`Giai thich quang hop nhu cho ban lop 6`), Evidence add. Dataflow logs to Prompt Log. Acceptance: feedback editable, demo-without-key works, build green.

**7.16 NewApps bundle — GradeForecaster, ExamMode, Budget, HabitSleep, CodeRunner, ResumeBuilder, PresentationCoach, LabReport, EssayOutliner, ImageOcclusion, FSRSSchedulerInfo, Viva, LanguageLab, ZoteroImport, PaperChat, DatasetFinder (all in `src/components/workspaces/NewAppsWorkspaces.tsx` via `App.tsx:65-82`).** Primary single file with many exports; edit per export surgically, never rewrite the whole file. Preserve each feature slice and its keys. Improvements per template: one Input-AI-Output line each in VI, Try Example, Evidence Snapshot, demo-without-key, Verify with key. Specific notes: GradeForecaster must warn `Du doan tham khao, khong phai diem that`; ExamMode must include integrity note `Khong gian lan`; Budget uses local math; HabitSleep shows streak; CodeRunner sandboxes and warns; ResumeBuilder anonymizes; PresentationCoach times 5-minute talks; LabReport structures method-result; EssayOutliner scaffolds thesis; ImageOcclusion builds flashcards; FSRS explains spacing; Viva practices defense with per-member turns; LanguageLab supports pronunciation without storing voice without consent; ZoteroImport confirms overwrite; PaperChat cites; DatasetFinder checks license. Dataflow each logs prompts. Acceptance: each lazy-chunked, each in palette and store, each with skeleton, empty, error, build green.

**7.17 PhET (`src/components/workspaces/PhETWorkspace.tsx`).** Primary file plus iframe to `https://*.phet.colorado.edu` allowed in CSP. Preserve simulation browser. Improvements: guided task card (`Quan sat -> Doan -> Kiem tra`), snapshot to evidence. Dataflow is iframe only. Acceptance: `IframeErrorBoundary` fallback shows, no CSP violation, build green.

**7.18 Pomodoro (`src/components/workspaces/PomodoroWorkspace.tsx`).** Primary file plus Zustand sprint (`DashboardHome.tsx:106`), `scc_pomo_completed_v1`, `scc_focus_sessions_log`, streak heatmap at `DashboardHome.tsx:112-120`. Preserve timer, sprint goal with floor 1, session log. Improvements: keep floor logic `Math.max(1, ...)`, lazy confetti, onboarding check hookup marking pomodoro true. Dataflow persists sessions. Acceptance: count persists, 4 of 4 hides checklist, break reminders, build green.

**7.19 QuizGenerator (`src/components/workspaces/QuizGeneratorWorkspace.tsx`).** Primary file plus `callGemini` quiz plus `scc_quiz_history_v1`. Preserve generation with difficulty and history. Improvements: difficulty labels `De`, `Vua`, `Kho` for THCS, diagram line, Try Example (`Tao 5 cau trac nghiem Khoa hoc lop 7`), Evidence add. Dataflow logs prompts. Acceptance: demo works without key, live verifies, history persists, build green.

**7.20 ModelTraining (`src/components/workspaces/ModelTrainingWorkspace.tsx`).** See Track 1 full spec. Preserve `trainedModels` Dexie usage. Improvements as in 6.1. Acceptance: under 30s train, confusion shown, reload persists, snapshot to dossier, build green.

**7.21 AcademicRadar (`src/components/workspaces/AcademicRadarWorkspace.tsx`).** Primary file plus analytics. Preserve radar insights over assignments and focus. Improvements: plain insights (`Tuan nay ban tap trung Toan tot`), no spoofed counts, empty state when no data, link to Evidence. Dataflow reads local stores. Acceptance: honest numbers only, charts accessible, build green.

**7.22 FewShotLab (`src/components/workspaces/FewShotLabWorkspace.tsx`).** See Track 2 full spec. Preserve pipeline and datasets wiring. Improvements as in 6.2. Acceptance: benchmark report exportable, Testing Log receives entry, build green.

**7.23 CitationVault (`src/components/workspaces/CitationVaultWorkspace.tsx`).** Primary file plus `scc_bibliography_v1` plus Dexie `bibliography`. Preserve citation capture and formatting. Improvements: license picker, copy citation button, link to dossier references. Dataflow persists. Acceptance: persists across reload, export works, build green.

**7.24 NotionImport (`src/components/workspaces/NotionImportWorkspace.tsx`).** Primary file plus frame to `notion.so`. Preserve import flow. Improvements: confirm before overwrite, show count, log. Dataflow is local. Acceptance: import reversible via trash, build green.

**7.25 Mermaid (`src/components/workspaces/MermaidWorkspace.tsx`).** Primary file plus `generateMermaidDiagram` agent action plus `mermaid` chunk (750k lazy, `vite.config.ts:82`). Preserve diagram render and export. Improvements: add Dossier Section 4 templates for `Input -> AI -> Output` with one-click insert, export PNG for dossier gallery. Dataflow logs prompts. Acceptance: lazy only here so bundle stays split, renders, exports, build green.

**7.26 StemLab (`src/components/workspaces/StemLabWorkspace.tsx`).** Primary file plus `StemChatTurn` at `gemini.ts:15`. Preserve STEM tutoring. Improvements: socratic hints, step-by-step toggle, Evidence add. Dataflow logs. Acceptance: demo plus verify, build green.

**7.27 UnitConverter (`src/components/workspaces/UnitConverterWorkspace.tsx`).** Primary file with local conversion. Preserve units and precision. Improvements: Try Example, swap button, history. Dataflow is offline. Acceptance: accurate, no network, build green.

**7.28 PeriodicTable (`src/components/workspaces/PeriodicTableWorkspace.tsx` plus `src/data/elementsData.ts`).** Primary files plus element data. Preserve table, search, detail. Improvements: VI names, quiz link to QuizGenerator, snapshot. Dataflow is static. Acceptance: searchable, accessible, build green.

**7.29 GroupProject (`src/components/workspaces/GroupProjectWorkspace.tsx`).** Primary file plus roles and tasks. Preserve member tasks and deadlines. Improvements: per-member lines export for 10-minute video, responsibility matrix, Evidence. Dataflow persists. Acceptance: exports text for scripts, build green.

**7.30 Desmos (`src/components/workspaces/DesmosWorkspace.tsx`).** Primary file plus `DesmosEquation` at `gemini.ts:14` plus `injectDesmosEquation` action plus frame to `desmos.com`. Preserve graph embed. Improvements: one-click equation insert from chat, snapshot. Dataflow is iframe. Acceptance: fallback shows, build green.

**7.31 CreationStudio (`src/components/workspaces/CreationStudioWorkspace.tsx` plus `CanvaStudioTab.tsx`).** Primary files plus frame to `canva.com`. Preserve creation flows. Improvements: link export validator for Bang A allowed platforms (Scratch, MIT App Inventor, Canva AI) with URL pattern check and open test. Dataflow stores links. Acceptance: validator blocks bad URLs, build green.

**7.32 CompetitionDossier (`src/components/workspaces/CompetitionDossierWorkspace.tsx`).** See Section 8 full 6-tab spec. Preserve draft persistence. Improvements as in Section 8. Acceptance: 8 editors plus estimator plus diagram plus gallery plus Drive validator plus print-to-PDF, build green.

**7.33 ScholarshipTracker (`src/components/workspaces/ScholarshipTrackerWorkspace.tsx`).** Primary file plus `scc_scholarship_v1`. Preserve tracking. Improvements: de-emphasize for Bang A with note `Danh cho sau nay`, keep functional. Dataflow persists. Acceptance: persists, build green.

**7.34 Arxiv (`src/components/workspaces/ArxivWorkspace.tsx` plus `searchArxiv` at `publicApis.ts:16`).** Primary files plus workbox cache. Preserve search with category and results. Improvements: plain summaries for middle school, save to CitationVault. Dataflow uses StaleWhileRevalidate. Acceptance: cached, empty and error states, build green.

**7.35 Excalidraw (`src/components/workspaces/ExcalidrawWorkspace.tsx`).** Primary file plus frame to `excalidraw.com`, deferred load with no static import per `vite.config.ts:81`. Preserve whiteboard. Improvements: export PNG to dossier gallery with caption. Dataflow is iframe plus export. Acceptance: lazy, fallback, build green.

**7.36 GeoGebra (`src/components/workspaces/GeoGebraWorkspace.tsx`).** Primary file plus frame to `geogebra.org`. Preserve geometry. Improvements: guided construction plus snapshot. Dataflow is iframe. Acceptance: fallback, build green.

**7.37 OpenLibrary (`src/components/workspaces/OpenLibraryWorkspace.tsx` plus `searchOpenLibrary` at line 95 plus `scc_openlib_prefill_v1`).** Primary files plus covers. Preserve book search. Improvements: citation add to vault, reading-level hint. Dataflow cached. Acceptance: cached, build green.

**7.38 Wolfram (`src/components/workspaces/WolframWorkspace.tsx`).** Primary file plus frame to `wolframalpha.com`. Preserve computational lookup. Improvements: step explainer in plain VI, snapshot. Dataflow is iframe. Acceptance: fallback, build green.

**7.39 PhotoMath (`src/components/workspaces/PhotoMathWorkspace.tsx` plus `MathDebugResult` at `gemini.ts:21`).** Primary file plus solver. Preserve math help. Improvements: socratic mode that shows steps before final answer, integrity note, Evidence. Dataflow logs. Acceptance: demo plus verify, build green.

**7.40 RetentionVault (`src/components/workspaces/RetentionVaultWorkspace.tsx`).** Primary file plus SRS decks `scc_srs_decks_v2` plus Dexie `srsCards` plus `createSRSDeck` action. Preserve spaced repetition. Improvements: daily reminder hookup, snapshot. Dataflow persists. Acceptance: scheduling works, build green.

**7.41 RubricChecker (`src/components/workspaces/RubricCheckerWorkspace.tsx` plus `RubricPreCheckResult` at line 19).** Primary files plus pre-check. Preserve rubric scoring. Improvements: map to Bang A rubric (suitability, understanding, diagram, tool use, creativity, presentation) with 1 to 5 sliders and suggestions. Dataflow logs. Acceptance: report adds to Testing Log, build green.

**7.42 DeadlineGantt (`src/components/workspaces/DeadlineGanttWorkspace.tsx`).** Primary file plus `createCalendarMilestones`. Preserve Gantt with milestones. Improvements: preset deadlines for `20/9`, `30/9`, `10/10`, `17/10`, `20-22/11` plus 48-hour stability window shading. Dataflow persists. Acceptance: presets load, build green.

**7.43 PeerQA (`src/components/workspaces/PeerQAWorkspace.tsx` plus `scc_peer_qa_v1`).** Primary file plus Q and A. Preserve peer questions. Improvements: anonymize ON by default, consent gate, moderation note. Dataflow persists locally. Acceptance: no PII leak, build green.

**7.44 DivisionAUI (`src/components/DivisionAUI.tsx`).** Primary file plus simplified shell. Preserve Division A mode. Improvements: make default when `?division=a` param present, large buttons, Input-AI-Output on every card, one-tap Evidence and Prompt Log. Dataflow is presentational. Acceptance: toggles with full UI without data loss, build green.

**7.45 Collaboration suite (`src/components/collab.tsx`: StudyRoomPanel, TeacherShareView, LectureCopilot, ApiDocsPanel, ExtensionHelper at `App.tsx:83-87`).** Primary file plus agent actions. Preserve study rooms, teacher share, lecture copilot, docs, extension helper. Improvements: apply Track 4 confirm and Undo plus read-only teacher share with no PII, plus Explain toggle. Dataflow logs. Acceptance: share links read-only, copilot logs, build green.

**7.46 Syllabus, deployment, and sync backends (`SyllabusParsedResult`, `DeployedSemesterResult` at `gemini.ts:12 and 23`, plus `DeploymentModal.tsx`, `AssignmentTrackerTab.tsx`, `CanvasSyncTab.tsx`, `GoogleSyncHubModal.tsx`, `DailyRadarTab.tsx`, `GmailRadarTab.tsx`).** Primary files plus `useSyncEngine` and `syncHooks`. Preserve sync engine with outbox and `updatedAt`. Improvements: anonymize logs, confirm before bulk sync, show sync status dots. Dataflow queues offline. Acceptance: no duplicate writes, retry works, build green.

Every feature above must be registered in AppStore plus CommandPalette with Vietnamese keywords, lazy via `lazyRetry` with Skeleton, Empty, Error, and success states, guest-mode degradation, quota handling, Prompt Log plus Evidence buttons, and 44px accessible controls. No feature is done until it meets the template and passes build, lint, and manual checks.

---
# 8. COMPLIANCE KIT — CompetitionDossierWorkspace 6 TABS

Extend `src/components/workspaces/CompetitionDossierWorkspace.tsx` (lazy at `src/App.tsx:95`). Persist draft to `scc_dossier_draft_v1` (new key, document in Appendix) plus Dexie `promptLogs` and `evidenceSnapshots`. Print-to-PDF via `window.print()` with `@page A4` CSS, no new dependencies. Vietnamese labels must match Mau 1 exactly. Prefill a school example such as `Truong THCS Cau Giay — Nhom Canh Dieu — De tai: Tro ly on tap tu vung tieng Anh bang AI` so students see a complete model. Require consent and anonymize defaults throughout. Block export until required checks pass, with clear messages explaining what is missing.

**8.1 Tab 1 Team (Doi thi).** Fields: member-count checkboxes for 1, 2, 3; leader info (full name, date of birth, class and school, commune and province, phone, email); second and third member same fields, hidden when count is lower; teacher or expert guide (full name, work unit, position, phone, email) labeled as guide only. Rules text: fixed team with no replacement except Committee approval; one team and one division per student per season; age 12 to 15 at deadline; single valid registration form. Validation: require leader name, valid date of birth within range with warning otherwise, valid phone and email patterns, required consent checkbox for data use, required declaration that each member joins only one team. Show age-at-deadline calculator (enter deadline `30/9/2026`, compute age). Acceptance: exports a cover page with signatures and date, validation blocks incomplete teams, build green.

**8.2 Tab 2 Dossier (8-section editors plus estimator plus diagram plus gallery plus Drive validator).** Eight editors mapping Mau 1 one to one. Section 1 problem plus reason: textarea with 300-character guidance, counter, Try Example filler about a close school problem. Section 2 users plus needs: chips for `Ban hoc`, `Thay co`, `Gia dinh`, `Cong dong` plus needs text. Section 3 data, prompts, tools plus roles: table with columns `Ten cong cu | Vai tro | Do team tu lam / AI ho tro / Ke thua mo`, plus add-row, plus import from Prompt Log. Section 4 diagram builder: Mermaid templates for `Du lieu dau vao -> AI xu ly -> Ket qua dau ra` with editable nodes, live preview via MermaidWorkspace renderer, export PNG to gallery. Section 5 test images: gallery sourced from `evidenceSnapshots` with caption field per image, blur toggle per image, before-and-after pairing. Section 6 demo functions and results: checklist of main functions plus result text plus link field for Scratch, MIT App Inventor, or Canva AI. Section 7 limits and next steps: two fields for current limits and near-term improvements. Section 8 Prompt Log plus Drive link: auto-filled summary from Tab 3 plus `https://drive.google.com/...` URL field with validator requiring `Anyone with link` checkbox plus Test-open button that opens in new tab. Page estimator: approximate `~1800 ky tu ~ 1 trang A4`, live total `So trang uoc tinh: X/8`, red warning when exceeding 8, per-section counts. Acceptance: estimator updates live, gallery inserts captioned images, diagram exports, Drive validator blocks submit when not shared, print includes all sections with page numbers, build green.

**8.3 Tab 3 Prompt Log store (Lich su cau lenh).** Viewer for Dexie `promptLogs` with filter by feature, search, sort by timestamp. Columns: time, feature, prompt truncated to 300 characters with expand, model, edited flag. Editors: `System Prompt` textarea, full conversation history import via paste JSON with validation, per-entry edit-notes field asking `Da sua gi sau khi AI tra loi?`, pledge sliders for percent self-built, AI-assisted, open-source inherited that must sum to 100 with live validation. Truthfulness checkbox: `To khai trung thuc toan bo cong cu, du lieu, ma tham khao da dung.` Buttons: Add manual entry, Export JSON, Copy summary for dossier Section 8. Auto-logging: every `callGemini` and Groq call appends automatically. Acceptance: auto-appends work, manual add works, sliders validate, export opens valid JSON, build green.

**8.4 Tab 4 Video scripts (5, 3, 10-minute plus teleprompter).** Generators with word-count and timer guidance: 5-minute presentation covering problem, idea, AI use, value (target 650 to 750 words, about 130 words per minute), 3-minute demo covering operation, functions, results (target 380 to 420 words), 10-minute regional covering all members with per-member lines like `Ban A: Gioi thieu van de (2 phut)`, `Ban B: Demo chuc nang (3 phut)`, `Ban C: Thu nghiem va bai hoc (2 phut)`, plus intro and closing. Teleprompter with large text, auto-scroll with speed control, 44px play, pause, reset, and timer with overtime warning. Checklist: all members appear in frame, stable audio, captions added, demo recorded on competition day for regional. Acceptance: word count plus timer update live, per-member lines export, teleprompter scrolls smoothly with reduced-motion respect, export as text works, build green.

**8.5 Tab 5 Testing Log (Nhat ky thu nghiem before-after).** Table with columns `Ngay | Tinh nang thu | Du lieu hoac Cau lenh | Ket qua truoc | Ket qua sau | Bai hoc rut ra`, plus before-and-after image pair upload sourced from evidence, plus `RubricChecker` pre-check embed with Bang A criteria scores. `Add Evidence Snapshot` from any workspace lands here automatically with timestamp. Require at least 2 iterations before Export enables, enforcing the show-iteration capability. Provide Try Example rows (example: `Lan 1: AI nhan dien sai anh mo — them 10 anh — Lan 2: dung 8/10`). Acceptance: table persists, images show with captions, rubric scores attach, export blocked until 2 rows complete, build green.

**8.6 Tab 6 Timeline, checklist, stability monitor, anonymize, license, prohibited self-check.** Timeline presets with dates: `20/9 free deadline`, `30/9 province and school`, `10/10 South regional`, `17/10 North and Central regional`, `20-22/11 Final Hanoi`, plus 48-hour stability window calculator where entering defense time shows `Bat dau giu on dinh tu: ...` and freezes guidance. Stability monitor: product URL field with ping every 5 minutes, status dot green or red, log to `scc_stability_pings_v1`, warning when down, export log. Anonymize controls: ON by default, blur faces toggle, redact names to initials, consent gate modal before saving identifiable media, one-click Clear personal media. License picker per resource: `Tu lam`, `MIT`, `CC-BY`, `Cong cong`, `Khong ro — can thay the` (blocks export when unclear). Prohibited self-check with 7 required unchecked boxes plus signature: no ghost test, no hired product, no teacher doing instead, no copy, no faked logs, no hidden sources, no illegal data; if any checked as violated, block export with guidance. Re-evaluation note: `Phuc khao trong 02 ngay lam viec, chi ap dung diem, loi ky thuat, lech quy trinh.` Export: print-to-PDF with cover plus 8 sections plus logs plus timeline plus stability summary, in VI with page numbers and table of contents. Acceptance: all checks must pass for Export to enable, stability log attaches, anonymize applied to images, build green.

Privacy and safety for the whole kit: anonymize ON by default, blur and redact components available, consent gate before image or voice storage, PII truncate to 300 characters in logs, license required per external resource, prohibited check blocks export on violation, Drive sharing explicitly confirmed as Anyone with link only for competition materials with a warning not to share personal folders.

---
# 9. DATA, OFFLINE, SECURITY, PWA, SEO, ACCESSIBILITY, TESTING

**9.1 Data layer and migrations.** Dexie `src/services/db.ts:10-80` is the source of truth. Versions 1 through 5 define the `version().stores()` chain; add version 6 only additively if you need new indexes, never alter historical versions. Tables: notes, srsCards, briefs, assignmentsQueue, bibliography, assignments, preferences, quota, trash, prompts, tokenUsage, dashboard, plus version 5 additions `promptLogs` with `id,feature,timestamp`, `evidenceSnapshots` with `id,feature,timestamp`, `trainedModels` with `id,type,savedAt`, `vaultIndex` with `docId,title`. Every write must set `updatedAt` to ISO string. Outbox pattern: queue offline mutations in `assignmentsQueue` and generic `preferences` pending sync, then flush when online. New code must use these tables; do not create parallel localStorage stores for prompts, evidence, models, or vault index. Document any new key in Appendix B. Acceptance: offline edit then reload preserves data; IndexedDB inspector shows tables with correct indexes; migration from v4 to v5 preserves data; lint clean.

**9.2 Offline and sync behavior.** Workbox in `vite.config.ts:39-53` already caches fonts, images, and APIs; add `api.nasa.gov` with StaleWhileRevalidate and 86400-second expiry if missing. Keep `navigateFallback` to `index.html` with denylist for `/api/`. Every lazy workspace needs Skeleton loading, EmptyState with guidance, and Error with Retry. `IframeErrorBoundary` must wrap all embeds (Desmos, GeoGebra, PhET, Wolfram, Canva, Excalidraw, Notion). Guest mode without Firebase must show `Dang dung che do khach — du lieu luu o may nay` and keep core study tools working. Sync engine (`useSyncEngine`, `syncHooks`) must queue with `updatedAt` and avoid duplicate writes. Acceptance: airplane mode keeps Home, notes, Pomodoro, converters working; AI shows demo-without-key; sync resumes on reconnect; build green.

**9.3 Security hardening checklist.** Content-Security-Policy in `vercel.json` must keep `default-src 'self'`; add `https://api.nasa.gov` to `connect-src` and keep `img-src https: data: blob:` for APOD; keep script, style, font, frame allowlists for Google, Firebase, generative language, Groq, Vercel, Desmos, GeoGebra, PhET, Wolfram, Canva, Excalidraw, Notion, Figma, OpenLibrary, arXiv. Keep HSTS `max-age=63072000; includeSubDomains; preload`, `X-Content-Type-Options: nosniff`, `X-Frame-Options: SAMEORIGIN`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(self), geolocation=()`. XSS: use `react-markdown` allowlist or sanitizer; never render raw AI HTML. Vault: AES-GCM PIN as in `gemini.ts:203-220`, sessionOnly option, plaintext warning. PII: 300-char truncation, anonymize children by default, consent gates. CORS: allowlist only; no wildcard. Secrets: never in URLs or commits; mask keys in UI. Acceptance: `npm run lint` clean, no `dangerouslySetInnerHTML` with AI text, no new fetch domain without CSP update, manual pen-test of Drive and API inputs, build green.

**9.4 PWA, performance, and SEO.** Keep manifest, shortcuts, share_target, icons, screenshots from `vite.config.ts:12-37`. Keep `manualChunks` plus `lazyRetry` so index stays near 592k and mermaid near 750k isolated to its workspace. Largest Contentful Paint: greeting plus Today Plan first, APOD images with `fetchPriority=low`, all non-critical images lazy. SEO in `index.html`: title `StudentOS — Tro ly hoc tap AI cho THCS (Bang A)`, description mentioning `ai.tainangviet.vn`, 8-page dossier helper, Prompt Log, Hanoi, Da Nang, Ho Chi Minh City dates, Open Graph tags, canonical live URL, theme-color. Analytics events: `dossier_export`, `prompt_log_add`, `evidence_snapshot`, `apod_toggle`, `model_train`, `benchmark_run`, each without personal data. Acceptance: Lighthouse PWA 100, Performance at least 85 on throttled 3G, install prompt works, shortcuts deep-link, build green.

**9.5 Accessibility conformance.** WCAG AA across all new and edited UI: contrast at least 4.5, focus ring `#D97757`, semantic headings, labels for every input, `aria-expanded`, `aria-pressed`, `aria-valuenow`, `role=progressbar`, `role=status`, `role=alert` where relevant, full keyboard operability, visible skip link, 44px targets, `prefers-reduced-motion` support, screen-reader announcements for APOD loading, onboarding progress, toasts, timers, teleprompter. Manual checklist per feature: tab order logical, Escape closes modals and drawers, progress announced, video scripts have captions reminder, color never the only signal. Test with keyboard only and with a screen reader for Home, Settings, Dossier, and Model Lab. Fix contrast failures before push.

**9.6 Testing strategy and suites.** `npm run test` equals `vitest run` (`package.json:13`). Existing suites in `src/services/`: `divisionA.test.ts`, `academic.test.ts`, `gemini.test.ts`, `persistence.test.ts`, `publicApis.test.ts`, `workflows.test.ts`. Add or extend tests for: onboarding progress and dismiss-today helpers, APOD cache fresh versus stale plus DEMO_KEY fallback plus toggle event, dossier page estimator math, prompt-log 300-char truncation, few-shot metric calculations, RAG vault clear, agent action allowlist. Keep tests fast and deterministic with mocked fetch and fake timers. Manual matrices required for: Home order on desktop and mobile, APOD card versus wallpaper versus video versus offline, Model Lab with 8 examples, Few-Shot benchmark, RAG with and without key, Agent confirm and Undo, Dossier export to PDF with 8-page warning, offline airplane test, quota exceeded test, Vietnamese versus English toggle. Update `ChangelogModal.tsx` with user-facing notes and keep counts honest by pasting command outputs.

---
# 10. BUILD ORDER 1-12 (DO IN ORDER, VERIFY EACH STEP)

**Step 1 Sync.** Run `git status --short`, `git fetch origin`, `git log --oneline HEAD..origin/main`, `git log --oneline -8`. If remote moved and newest commit is not yours, pull with `git pull --no-rebase origin main`, review diff for resurrected banners, duplicate key writers, changed service signatures, then `npm install`, baseline `npm run build` exit 0. Record absorbed commits and baseline hash. Never force-push. Repeat every session and before every push.

**Step 2 Recon.** Run Glob `src/**/*.tsx`, search for `OnboardingChecklist|nasa_apod|NasaApod|fetchNasa|APOD|callGemini|Prompt Log`, list `src/services/`, `src/hooks/`, `src/data/`, grep `scc_` keys and `VITE_` env vars, read `package.json`, `AGENTS.md`, `App.tsx` head, `DashboardHome` head, `OnboardingChecklist`, `useNasaApod`, `publicApis` APOD section, `gemini` key and caller sections, `db` tables, `vite.config` chunks, `vercel.json` headers. Paste key counts and env list into working notes. Do not invent missing paths; report gaps.

**Step 3 Home onboarding fix.** Move checklist below Today Plan near `App.tsx:3267`, implement collapsed drawer with `scc_onboarding_collapsed_v1`, hide on 4 of 4, keep auto-detect, add Try Example. Verify greeting plus Today Plan above fold, Vitest for progress and dismiss, `npm run build` exit 0.

**Step 4 APOD repair.** Apply the 10-step repair across `useNasaApod.ts:30-77`, `publicApis.ts:224`, `DashboardHome.tsx:188-535`, `AccountSettingsModal.tsx:173-217`. Test DEMO_KEY 429, video day, offline, multitab sync, Clear cache. Update Changelog. Verify instant toggle without reload plus Retry, `npm run build` exit 0.

**Step 5 ModelTraining Track 1.** Build the on-device classifier lab with 8-plus examples, under 30s train, accuracy plus confusion, IndexedDB save, export, snapshot, consent. Verify reload persistence and gallery flow. `npm run build` exit 0.

**Step 6 FewShot Track 2.** Build calibration UI with benchmark report and Add-to-dossier. Verify metrics and log flow. `npm run build` exit 0.

**Step 7 RAG Track 3.** Build vault index with Sources, consent, Clear index, demo-without-key. Verify cited answer and empty states. `npm run build` exit 0.

**Step 8 Agent Track 4.** Add 2 safe actions plus confirm, Undo, Explain. Verify apply and restore plus blocklist. `npm run build` exit 0.

**Step 9 Dossier Compliance Kit.** Build 6 tabs with 8 editors, estimator, diagram, gallery, Drive validator, Prompt Log store, video scripts with teleprompter, Testing Log with 2-iteration gate, Timeline with stability monitor, anonymize, license, prohibited check, print-to-PDF. Verify export blocked until complete. `npm run build` exit 0.

**Step 10 Polish all workspaces.** Apply Section 7 template to every workspace and component including Navbar, Sidebar, Shell, CommandPalette, Omnibox, FloatingCopilot, StudyAssistantChat, AppStore, Settings, GeminiSuite, Landing, DocumentHub, Timetable, PdfReader, Feynman, NewApps bundle, PhET, Pomodoro, Quiz, ModelTraining, AcademicRadar, FewShot, Citation, Notion, Mermaid, StemLab, UnitConverter, PeriodicTable, GroupProject, Desmos, CreationStudio, CompetitionDossier, Scholarship, Arxiv, Excalidraw, GeoGebra, OpenLibrary, Wolfram, PhotoMath, RetentionVault, RubricChecker, DeadlineGantt, PeerQA, DivisionAUI, collab suite, syllabus and sync backends. Register each in store and palette with VI keywords. Verify skeletons, empty, error, confetti, guest, quota, logs. `npm run build` exit 0.

**Step 11 Harden.** Dexie v6 only if needed and additively, CSP and workbox for APOD, SEO and analytics events, WCAG pass, EN plus VI strings, Changelog update, lint and tests clean. `npm run build`, `npm run lint`, `npm run test` all green.

**Step 12 Verify and push.** Run `git fetch origin`, `git log --oneline HEAD..origin/main`, pull with `--no-rebase` if needed, resolve coherently, `npm run build` exit 0, `npm run lint`, `npm run test`, `wc -w`, then `git push origin main`. Report build passed plus pushing to Vercel plus absorbed commits plus dossier demo link plus test summary. Never force-push. If build fails, fix surgically within 3 loops and re-verify.

If you are blocked, keep the current step `in_progress`, add a follow-up todo describing the blocker, and report honestly. Do not skip steps. Do not reorder without reason. Keep diffs small and messages clear.

---
# APPENDIX — FILE MAP, KEYS, ENV VARS, GLOSSARY

**A. File map with verified lines.** Root: `package.json:3 v2.4.2`, `package.json:9 build`, `package.json:12 lint`, `package.json:13 test`, `AGENTS.md:1-24`, `vite.config.ts:1-94` with PWA at 9-54 and chunks at 75-86 and limit at 91, `vercel.json` with redirects, rewrites, CSP, HSTS, `index.html` with SEO and PWA, `server.ts` with Express. App: `src/App.tsx:1-3666` with `lazyRetry` at 21-36, lazies at 37-100, `WorkspaceId` at 128, `activeWorkspace` at 384-393, `OnboardingChecklist` lazy at 92 and usage near 3267. Home: `src/components/DashboardHome.tsx:1-550` with greeting 56-61, name 64-79, intention 82-93, vibe 96-103, sprint 106-109, streak 112-120, `useNasaApod` at 17 and 175, mode at 177-182, wallpaper gate at 189, card section at 485-535. Onboarding: `src/components/OnboardingChecklist.tsx:1-116` with keys at 9-10 and 28, helpers at 13-20, states at 27-41, persist at 43-52, auto-detect at 55-67, progress at 68-89, list at 93-104, dismiss at 105-111. APOD hook: `src/hooks/useNasaApod.ts:1-80`. Public APIs: `src/services/publicApis.ts:1-261` with arXiv at 16-80, OpenLibrary at 95-120, APOD constants at 170-172, env at 176, DEMO_KEY at 179, cache at 193-206, fetch V2 at 224, wrapper at 260. Settings: `src/components/AccountSettingsModal.tsx` with genai import at 53, test states at 151-152 and 175-176, enabled check at 173, preview at 192 and 205, test at 200-207, clear at 217, Gemini test at 265-281, NASA section near 934-965. Gemini: `src/services/gemini.ts:1-1647` with keys at 28-29, get and set at 31-62, test at 64-76, limiter at 79, Vault at 203-220, Groq at 255-303 with `llama-3.3-70b-versatile` at 295, caller at 305-368, helpers at 414-798. Database: `src/services/db.ts:1-161` with tables at 10-21 and versions at 25-79 and new tables at 75-78. Stores: `workspaceStore.ts`, `pomodoroStore.ts`. Hooks: `useBadgeCounts`, `useDebouncedCallback`, `useSyncEngine`, `useWorkspaceRouter`, `useKeyboardShortcuts`, `useAppRouter`, `useBulkSelect`. Data: `aiTrainingDatasets.ts`, `quotes.ts` with `getTodayQuote` and `QUOTE_BANK`, `elementsData.ts`, `mockData.ts`. Services: `aiTrainingPipeline.ts`, `promptLog.ts`, `ragVault.ts`, `evidence.ts`, `vault.ts`, `theme.ts`, `firebase.ts`, `googleAuth.ts`, `googleClassroom.ts`, `googleWorkspace.ts`, `canvas.ts`, `moodle.ts`, `i18n.ts`, `analytics.ts`, `srsEngine.ts`, `bibliography.ts`, `trash.ts`, `ambientAudio.ts`, `aiRouter.ts`, `academic.ts`. Components: `Navbar`, `Sidebar`, `Omnibox`, `CommandPalette`, `FloatingAiCopilot`, `StudyAssistantChat`, `AppStoreModal`, `ChangelogModal`, `DivisionAUI`, `collab`, `EmptyState`, `Skeleton`, `ConfirmationModal`, `ErrorBoundary`, `IframeErrorBoundary`, `LandingPage`, `ShortcutsModal`, `PrivacyModal`, plus tabs `DailyRadar`, `GmailRadar`, `AssignmentTracker`, `CanvasSync`, `GoogleDrive`, `GoogleClassroomPanel`, `MoodlePanel`, plus modals `StudyCard`, `QuickDraft`, `PortfolioExport`, `MorningCheckIn`, `ScheduleStudy`, `StudyPlanGenerator`, `Deployment`, `ApiActivation`, `OAuthGuide`, `GeminiSettings`, `GoogleSyncHub`, `OnboardingWizard`, `InteractiveIntro`, `WhyIsThisHard`, `FeedbackWidget`, `Toast`, `SplitScreenStudio`, `NotebookLMStudio`, `FlashcardStudio`, `CanvaStudio`. Workspaces: `DocumentHub`, `Timetable`, `PdfReader`, `Feynman`, `NewAppsWorkspaces` bundle, `PhET`, `Pomodoro`, `QuizGenerator`, `ModelTraining`, `AcademicRadar`, `FewShotLab`, `CitationVault`, `NotionImport`, `Mermaid`, `StemLab`, `UnitConverter`, `PeriodicTable`, `GroupProject`, `Desmos`, `CreationStudio`, `CompetitionDossier`, `ScholarshipTracker`, `Arxiv`, `Excalidraw`, `GeoGebra`, `OpenLibrary`, `Wolfram`, `PhotoMath`, `RetentionVault`, `RubricChecker`, `DeadlineGantt`, `PeerQA`. Tests: `divisionA.test.ts`, `academic.test.ts`, `gemini.test.ts`, `persistence.test.ts`, `publicApis.test.ts`, `workflows.test.ts`, plus `academic.ts` helper. Layouts: `Shell.tsx`. Entry: `main.tsx`.

**B. LocalStorage keys registry (canonical, do not rename).** User and home: `scc_user_preferred_name`, `scc_user_daily_intention`, `scc_user_selected_vibe`, `scc_user_sprint_goal`, `scc_focus_sessions_log`, `scc_pomo_completed_v1`, `scc_pomo_scratchpad_v1`, `scc_pomodoro_streak_today`, `scc_last_morning_checkin`, `scc_streak_history`. Tasks and notes: `scc_user_assignments_v2`, `scc_markdown_notes_v1`, `scc_flashcard_decks_v1`, `scc_srs_decks_v2`, `scc_quiz_history_v1`, `scc_timetable_v1`, `scc_bibliography_v1`, `scc_scholarship_v1`, `scc_peer_qa_v1`. Navigation: `scc_active_workspace_v1`, `scc_active_tab_v1`, `scc_pinned_apps_v2`, `scc_recent_tabs_v1`, `scc_recent_palette_v1`, `scc_app_usage_v1`, `scc_sidebar_expanded`, `scc_workspace_store`, `scc_saved_layouts`, `scc_splitscreen_config_v1`, `scc_split_quad_mode`, `scc_quad_tools`, `scc_dashboard_personalize_open`, `scc_tour_seen`, `scc_tour_seen_v2`, `scc_onboarding_picks`. Onboarding: `scc_onboarding_checks_v1`, `scc_onboarding_collapsed_v1`, `scc_onboarding_dismissed_date_v1`. Theme and UI: `scc_theme`, `scc_explicit_theme_v1`, `scc_auto_system_theme_v1`, `scc_color_theme_v1`, `scc_ui_density_v1`, `scc_reduce_motion_v1`, `scc_reduced_motion`, `scc_user_avatar_v1`. AI keys and quota: `scc_gemini_api_key`, `scc_gemini_api_key_session`, `scc_groq_api_key`, `scc_gemini_daily_quota_v1`. Integrations: `scc_canvas_settings_v1`, `scc_master_sheet_id`, `scc_cached_classroom_assignments`. NASA: `scc_enable_nasa_apod`, `scc_nasa_apod_cache`, `scc_nasa_apod_mode`. Prompt and evidence: `scc_prompt_log_v1`, `scc_rag_vault_enabled`, `scc_openlib_prefill_v1`, `scc_shortcut_settings`, `scc_stability_pings_v1`, `scc_lecture_scratchpad`, `scc_cookie_test`. Session: `scc_chunk_reload_v1` in sessionStorage. New allowed only if documented: `scc_apod_last_fetch` for APOD timestamp, `scc_dossier_draft_v1` for dossier draft, `scc_prompt_logs_index` for log index, `scc_evidence_gallery_v1` for gallery order, `scc_model_lab_v1` for lab state. Single writer per key; no duplicates.

**C. Environment variables registry.** Client `VITE_` keys: `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_APP_ID`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_MEASUREMENT_ID`, `VITE_FIREBASE_MESSAGING_SENDER_ID`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_STORAGE_BUCKET`, `VITE_GEMINI_API_KEY`, `VITE_GROQ_API_KEY`, `VITE_GOOGLE_CLIENT_ID`, `VITE_GOOGLE_PROJECT_ID`, `VITE_NASA_API_KEY`. Server keys: `GEMINI_API_KEY`, `GROQ_API_KEY` for `/api/gemini/generate`. Behavior: missing NASA key falls back to `DEMO_KEY` with rate limits; missing Gemini key enables demo-without-key mode; missing Groq key disables failover but keeps server path. Never commit `.env`; set vars in Vercel dashboard. Document overrides in Settings help text.

**D. Glossary for Bang A in Vietnamese plus English (Dieu 5 exact meanings).** AI (Tri tue nhan tao): technology letting computers simulate human abilities like recognition, data analysis, prediction, content creation, decision support, task automation. Prompt Log (Lich su cau lenh): record of prompts, requests, exchanges, instructions used with AI tools while building, testing, editing, completing the product, including System Prompt and full conversation history. Dataset (Bo du lieu): collection used to build, test, train, fine-tune, evaluate AI models or systems. API (Giao dien lap trinh ung dung): technical method letting software and services connect and exchange data. GitHub and GitLab: platforms storing, managing source code and tracking development. Repository (Kho luu tru ma nguon): place storing code, docs, edit history on GitHub or GitLab. Commit history (Lich su thay doi ma nguon): record of edits and updates during development. No-code and low-code: platforms building software with no or very little code. AI-assisted coding (Lap trinh co su ho tro cua AI): using AI to suggest, write, check, fix, optimize code; team must still understand, verify, edit, take responsibility. MVP (Minimum Viable Product, San pham kha dung toi thieu): initial version with core functions enough to demo, test, improve. Deploy (Trien khai): putting product online so organizers, jury, users can access and test. Hosting: service or platform storing and running products online. RAG (Retrieval-Augmented Generation, Tao sinh co tang cuong truy xuat): method combining generative AI with external retrieval for grounded answers. AI Agent (Tac nhan AI): AI system performing tasks toward goals, combining tools and steps. Baseline and Ablation: Baseline is initial comparison model; Ablation removes or changes a component to measure contribution. AI evaluation metrics (Chi so danh gia): quantitative or qualitative indicators for accuracy, stability, safety, output quality. Workflow (Quy trinh tac vu): ordered steps, tools, tasks to achieve a goal. Keep this glossary visible in Dossier help and DivisionAUI.

**E. Non-goals and stop conditions.** Do not rebuild authentication, Canvas LMS sync, or Google Workspace sync from scratch; preserve and harden existing flows. Do not claim LLM training, awards, or metrics you did not measure. Do not perform full-file rewrites over 200 lines. Do not push without a green build. Do not force-push. Do not store personal image, voice, or health data without consent. Do not hide AI assistance; declare it. Stop and ask if a required file is missing, if two agents conflict on the same key, or if a rule is ambiguous; cite the rule and propose options.

Start now at Step Zero. End with a green build, a passing test run, a verified word count, and a push to `main` with a dossier demo ready for `ai.tainangviet.vn`, regional improvement sessions on 10/10 South and 17/10 North and Central, and the final in Hanoi on 20-22/11. Make every screen explainable by a 13-year-old in under three minutes.

